# 4. 存储与检索
- 在最基础的层面上，数据库需要做两件事：当你给它一些数据时，它应该存储这些数据；当你之后再询问时，它应该把数据返回给你。在本章中，我们从数据库的角度讨论同样的问题：数据库如何存储你提供的数据，以及当你请求时如何再次找到这些数据。并且考虑不同存储请求方式的优化。
- 本章首先研究两种用于 OLTP 的存储引擎家族：写入不可变数据文件的 _日志结构_ 存储引擎，以及像 _B 树_ 这样就地更新数据的存储引擎。这些结构既用于键值存储，也用于二级索引。随后在 [“分析型数据存储”](https://ddia.vonng.com/ch4/#sec_storage_analytics) 中，我们将讨论一系列针对分析优化的存储引擎；在 [“多维索引与全文索引”](https://ddia.vonng.com/ch4/#sec_storage_multidimensional) 中，我们将简要介绍用于更高级查询（如文本检索）的索引。
## OLTP 系统的存储与索引

| 概念          | 说明                         |
| ----------- | -------------------------- |
| **OLTP**    | 在线事务处理，面向大量小规模读写（如用户注册、下单） |
| **日志（Log）** | 仅追加的磁盘文件，写入高效              |
| **索引**      | 从主数据派生的额外结构，用于加速读取         |

- 最简单的存储：两个Bash函数

```bash
db_set () {
  echo "$1,$2" >> database   # 追加到文件末尾
}

db_get () {
  grep "^$1," database | sed -e "s/^$1,//" | tail -n 1  # 扫描整个文件
}
```

**使用示例**：

```bash
$ db_set 12 '{"name":"London","attractions":["Big Ben","London Eye"]}'
$ db_set 42 '{"name":"San Francisco","attractions":["Golden Gate Bridge"]}'
$ db_get 42
{"name":"San Francisco","attractions":["Golden Gate Bridge"]}
```

**多次更新同一个key**：

```bash
$ db_set 42 '{"name":"San Francisco","attractions":["Exploratorium"]}'
$ db_get 42
{"name":"San Francisco","attractions":["Exploratorium"]}

$ cat database
12,{"name":"London","attractions":["Big Ben","London Eye"]}
42,{"name":"San Francisco","attractions":["Golden Gate Bridge"]}
42,{"name":"San Francisco","attractions":["Exploratorium"]}
```

- 存储：日志（Log）
	- **原理**：每次写入直接追加到文件末尾
	- **优点**：写入非常高效（O(1)）
	- **真实数据库**：同样使用仅追加的数据文件作为日志
	- **额外处理**：并发写入、空间回收、崩溃恢复
> 注：日志不一定是人类可读的文本，可以是二进制的。
-  **索引的作用**：用额外结构（如排序、哈希）加速查找。
-  索引的代价

| 代价类型 | 说明 |
|----------|------|
| **写入变慢** | 每次写入不仅要写数据，还要更新索引 |
| **占用空间** | 索引消耗额外磁盘空间 |
| **需要手动选择** | 数据库不会自动为所有字段建索引，由开发者根据查询模式选择 |

- 权衡
> 	**索引加快读查询，但减慢写入速度。**
	- 日志（仅追加）是最快的写入方式
	- 任何索引都会破坏这种"纯粹"的写入性能
	- 需要根据应用的查询模式，选择收益最大的索引
- 总结：**日志（仅追加文件）= 写入快。索引 = 读取快，但写入变慢且占空间。OLTP系统需要你在两者之间做权衡。**
### 日志结构存储
在仅追加的日志文件中存储键值对时，可以在内存中维护一个哈希映射，记录每个键最新值的字节偏移量，从而加快读取速度。写入时更新映射，读取时通过映射定位并读取值（若数据在缓存中则无需磁盘I/O）。
如 [图 4-1](https://ddia.vonng.com/ch4/#fig_storage_csv_hash_index) 所示。

![](https://ddia.vonng.com/fig/ddia_0401.png)

图 4-1. 以类似 CSV 格式存储键值对日志，使用内存哈希映射建立索引。
但该方法存在以下问题：
- **磁盘空间无法释放**：旧日志条目不会被清理，长期写入可能耗尽空间。
- **重启慢**：哈希映射不持久化，重启需扫描整个日志文件重建索引。
- **内存限制**：哈希表必须存于内存，磁盘哈希表因随机I/O、扩容成本高和冲突处理复杂而难以高效实现。
- **范围查询效率低**：无法高效扫描键范围，只能逐个查找。
#### SSTable 文件格式
**SSTable（排序字符串表）**是一种按键排序存储键值对的文件格式，每个键在文件中只出现一次。
其核心特点是：
- 将键值对按顺序分组为若干**块**（如几千字节）
- 在内存中维护一个**稀疏索引**，只记录每个块的第一个键
- 索引可使用 B 树、字典树等结构单独存储
**查找过程**（以键 `handiwork` 为例）：
1. 在稀疏索引中定位到相邻的两个键：`handbag` 和 `handsome`
2. 根据排序规则，`handiwork` 应位于二者之间
3. 寻址到 `handbag` 所在的块偏移量
4. 从该位置开始顺序扫描，直到找到目标键或确认不存在
![](https://ddia.vonng.com/fig/ddia_0402.png)

图 4-2. 带有稀疏索引的 SSTable，允许查询跳转到正确的块。
**优点**：
- 内存中只需保存稀疏索引，大幅降低内存占用
- 每个数据块可压缩，节省磁盘空间并减少 I/O 带宽（以少量 CPU 为代价）
- 范围查询高效，支持快速跳转
#### 构建和合并 SSTable
**内存表 + 磁盘SSTable段 + 后台合并**。
- 1. 写入与构建
	- **内存表**：写入操作先进入内存中的有序结构（如红黑树、跳表），支持任意顺序插入。
	- **持久化日志**：同时将写入追加到磁盘日志，用于崩溃后恢复内存表。
	- **刷盘**：内存表达到阈值后，将其排序后作为SSTable文件写入磁盘（成为新“段”），随后释放内存并丢弃对应日志。
-  2. 读取
	按顺序查找：**内存表 → 最新磁盘段 → 较旧段**，直到找到键或确认不存在。
- 3. 合并与压实（后台进行）
	- 类似归并排序：同时读取多个段文件，每次取最小键写入新段。
	- 同一键出现多次时，**只保留最新值**。
	- 删除通过写入“墓碑”记录实现，合并时丢弃已删除键的旧值。
	- 如 [图 4-3](https://ddia.vonng.com/ch4/#fig_storage_sstable_merging) 所示：并排开始读取输入文件，查看每个文件中的第一个键，将最低的键（根据排序顺序）复制到输出文件，然后重复。如果同一个键出现在多个输入文件中，只保留较新的值。这会产生一个新的合并段文件，也按键排序，每个键只有一个值，并且它使用最少的内存，因为我们可以一次遍历一个键的 SSTable。

![](https://ddia.vonng.com/fig/ddia_0403.png)

图 4-3. 合并多个 SSTable 段，仅保留每个键的最新值。
- 4. 关键优势
	- **段文件不可变**：简化崩溃恢复（未完成的SSTable可直接丢弃）。
	- **支持对象存储**：不限于本地磁盘（如SlateDB、Delta Lake）。
	- **成熟应用**：RocksDB、Cassandra、HBase等均采用此算法。
#### 布隆过滤器
工作原理
- **构建**：将 SSTable 中每个键通过哈希函数映射到位数组的多个位置，并将这些位设为 1。
- **查询**：对目标键计算相同哈希，检查对应位：
    - **至少一位为 0** → 键**肯定不在**该 SSTable 中（可安全跳过）
    - **所有位均为 1** → 键**很可能存在**（可能出现**假阳性**）
- [图 4-4] 显示了一个包含两个键和 16 位的布隆过滤器示例（实际上，它会包含更多的键和更多的位）。对于 SSTable 中的每个键，我们计算一个哈希函数，产生一组数字，然后将其解释为位数组的索引 [14](https://ddia.vonng.com/ch4/#fn:14)。我们将对应于这些索引的位设置为 1，其余保持为 0。例如，键 `handbag` 哈希为数字 (2, 9, 4)，所以我们将第 2、9 和 4 位设置为 1。然后将位图与键的稀疏索引一起存储为 SSTable 的一部分。这需要一点额外的空间，但与 SSTable 的其余部分相比，布隆过滤器通常很小。

![](https://ddia.vonng.com/fig/ddia_0404.png)

图 4-4. 布隆过滤器提供了一种快速的概率检查，用于判断特定键是否存在于特定 SSTable 中。
 关键特性
- **假阳性**：所有位为 1 但键实际不存在（由其他键的位巧合造成）
- **无假阴性**：若布隆过滤器说“不存在”，则 100% 不存在
- **空间与精度权衡**：每个键约 10 位 → 1% 假阳性率；每增加 5 位/键 → 假阳性率降低 10 倍
 在 LSM 中的价值
- **跳过无关段**：快速排除不包含该键的 SSTable，减少不必要的磁盘扫描
- **假阳性无害**：仅造成少量额外工作，继续查找下一段即可
布隆过滤器极小（相对 SSTable），利用位运算可极快判断，是 LSM 树读取优化的重要手段。
#### 压实策略
1. 分层压实（Size-tiered Compaction）
**工作机制**：
- 较小、较新的SSTable逐步合并成较大、较旧的SSTable
- 类似大小的段被合并成一个更大的段
**特点**：
- **优点**：能处理非常高的写入吞吐量
- **缺点**：旧数据段可能变得非常大，合并时需要大量临时磁盘空间
2. 分级压实（Leveled Compaction）
**工作机制**：
- 将键范围分成多个较小的SSTable  
- 旧数据被移动到独立的"级别"（levels）中
- 以更增量的方式进行压实
**特点**：
- **优点**：比分层策略使用更少的磁盘空间，读取更高效（需检查的SSTable更少）
- **缺点**：写入放大相对较高
**打个比方：**
- **分层压实**：像是用**卡车**拉货。等货物攒够一整车再出发。虽然车次少，但每趟拉得多，**运输效率（写入吞吐量）**很高。
- **分级压实**：像是**频繁发车的小面包车**。每来一点货就立刻送走。车次频繁，对**查询（读取）** 友好，但运输成本高，拉新货的效率不如卡车。
策略选择建议

| 工作负载       | 推荐策略 | 原因        |
| ---------- | ---- | --------- |
| **写多读少**   | 分层压实 | 写入吞吐量更高   |
| **读多写少**   | 分级压实 | 读取效率更好    |
| **频繁写少量键** | 分级压实 | 避免大量小文件合并 |
| **批量写大量键** | 分层压实 | 合并效率高     |
 **核心思想**：无论选择哪种策略，LSM树本质都是在后台维护不断合并的SSTable级联结构，实现高效的写入和可接受的读取性能。
### B 树
我们之前看到的日志结构索引将数据库分解为可变大小的 _段_，通常为几兆字节或更大，写入一次后就不可变。相比之下，B 树将数据库分解为固定大小的 _块_ 或 _页_，并可能就地覆盖页。页传统上大小为 4 KiB，但 PostgreSQL 现在默认使用 8 KiB，MySQL 默认使用 16 KiB。

每个页都可以使用页号来标识，这允许一个页引用另一个页 —— 类似于指针，但在磁盘上而不是在内存中。如果所有页都存储在同一个文件中，将页号乘以页大小就给我们文件中页所在位置的字节偏移量。我们可以使用这些页引用来构建页树，如 [图 4-5](https://ddia.vonng.com/ch4/#fig_storage_b_tree) 所示。

![](https://ddia.vonng.com/fig/ddia_0405.png)

图 4-5. 使用 B 树索引查找键 251。从根页开始，我们首先跟随引用到键 200–300 的页，然后是键 250–270 的页。

一个页被指定为 B 树的 _根_；每当你想在索引中查找一个键时，你就从这里开始。该页包含几个键和对子页的引用。每个子页负责一个连续的键范围，引用之间的键指示这些范围之间的边界在哪里。（这种结构有时称为 B+ 树，但我们不需要将其与其他 B 树变体区分开来。）

在 [图 4-5](https://ddia.vonng.com/ch4/#fig_storage_b_tree) 的例子中，我们正在查找键 251，所以我们知道我们需要跟随边界 200 和 300 之间的页引用。这将我们带到一个看起来相似的页，该页进一步将 200–300 范围分解为子范围。最终我们到达包含单个键的页（_叶页_），该页要么内联包含每个键的值，要么包含对可以找到值的页的引用。

B 树的一个页中对子页的引用数称为 _分支因子_。例如，在 [图 4-5](https://ddia.vonng.com/ch4/#fig_storage_b_tree) 中，分支因子为六。实际上，分支因子取决于存储页引用和范围边界所需的空间量，但通常为几百。

如果你想更新 B 树中现有键的值，你搜索包含该键的叶页，并用包含新值的版本覆盖磁盘上的该页。如果你想添加一个新键，你需要找到其范围包含新键的页并将其添加到该页。如果页中没有足够的空闲空间来容纳新键，则页被分成两个半满的页，并更新父页以说明键范围的新细分。

![](https://ddia.vonng.com/fig/ddia_0406.png)

图 4-6. 通过在边界键 337 上分割页来增长 B 树。父页被更新以引用两个子页。

在 [图 4-6](https://ddia.vonng.com/ch4/#fig_storage_b_tree_split) 的例子中，我们想插入键 334，但范围 333–345 的页已经满了。因此，我们将其分成范围 333–337（包括新键）的页和 337–344 的页。我们还必须更新父页以引用两个子页，它们之间的边界值为 337。如果父页没有足够的空间容纳新引用，它也可能需要被分割，分割可以一直持续到树的根。当根被分割时，我们在它上面创建一个新根。删除键（可能需要合并节点）更复杂 [5](https://ddia.vonng.com/ch4/#fn:5)。

这个算法确保树保持 _平衡_：具有 _n_ 个键的 B 树始终具有 _O_(log _n_) 的深度。大多数数据库可以适合三或四层深的 B 树，所以你不需要跟随许多页引用来找到你要查找的页。（具有 500 分支因子的 4 KiB 页的四层树可以存储多达 250 TB。）
#### 使 B 树可靠
**核心问题**：B树直接覆盖磁盘上的页（原地修改）。如果在写入过程中崩溃，可能造成数据损坏（如撕裂页、孤立页）。
**解决方案：预写日志（WAL）**
- 修改B树之前，先将所有操作写入一个仅追加的日志文件
- 日志写入磁盘后，才能修改B树页
- 崩溃恢复时，重放日志即可恢复到一致状态。在文件系统中，等效机制称为 _日志记录_（_journaling_）。
**内存缓冲 + WAL**
- B树页先在内存中缓存，不立即写盘
- 但WAL必须用`fsync()`强制刷盘
- 确保即使崩溃，日志中的操作也不会丢失
**对比LSM**：LSM只追加文件、不原地修改，天然更易恢复；B树依赖WAL实现原子性和持久性。
#### B 树变体
**写时复制（如LMDB）**
- 不覆盖原有页，也不使用WAL
- 修改的页写入新位置，父页更新指向新位置
- 好处：崩溃恢复简单，也利于实现快照隔离
**键缩写**
- 内部页不存完整键，只存足够区分范围的边界信息
- 每页能塞更多键 → 分支因子更高 → 树层数更少
**叶页顺序布局**
- 让叶页在磁盘上物理连续排列
- 好处：范围扫描时减少磁盘寻道
- 难点：树增长后很难维持这种顺序
**横向指针（B+树）***
- 叶页增加指向左右兄弟页的指针
- 顺序扫描时无需跳回父页，直接横向遍历
### 比较 B 树与 LSM 树
作为经验法则，LSM 树更适合写入密集型应用，而 B 树对读取更快。存储引擎有时会混合两种方法的特征，例如具有多个 B 树并以 LSM 风格合并它们。
#### 读取性能
**点查询**
- **B树**：层级少，每层读一页，性能稳定可预测
- **LSM**：需检查多个SSTable，但布隆过滤器可跳过无关段，减少磁盘I/O
**范围查询**
- **B树**：利用树的排序结构，简单快速
- **LSM**：需并行扫描所有段并合并结果，布隆过滤器无法帮助范围查询，因此比点查询昂贵得多
**写入压力影响**
- LSM在高写入吞吐量下，若压实跟不上写入速度，内存表填满会导致延迟峰值
- 许多存储引擎（如RocksDB）会执行**背压**：暂停读写直到内存表刷盘完成
**并行读取**
- 现代SSD（特别是NVMe）支持大量并行读请求
- 两种引擎都能提供高读取吞吐量，但需要精心设计以利用并行性
#### 顺序与随机写入
**B树：随机写入**
- 键分散在整个空间，需要覆盖不同位置的页
- 每次写入都是小规模、分散的磁盘操作
- 写入吞吐量较低
**LSM树：顺序写入**
- 写入整段文件（内存表刷盘或压实合并）
- 每次写入规模较大、连续
- 写入吞吐量较高
**硬件差异**
- **HDD**：顺序 vs 随机差距巨大（机械寻道需几毫秒）
- **SSD**：差距缩小但仍明显
**SSD上随机写入慢的原因**
- 闪存特性：读/写单位是页（4KB），擦除单位是块（512KB）
- 随机写入导致块内混合有效和无效数据
- 擦除前需搬移有效数据（垃圾回收 GC）
- GC消耗带宽，增加写放大，加速闪存磨损
#### 写放大
**写放大**指数据库执行一次应用写入时，底层磁盘实际发生的I/O操作倍数。计算方式：实际写入磁盘的总字节数 ÷ 仅追加日志的写入字节数。
**LSM树的写放大来源**：
- 先写日志（持久性）
- 内存表刷盘时再写一次
- 每次压实又会重写数据
**B树的写放大来源**：
- 写预写日志（WAL）一次
- 写树页一次
- 即使只改几个字节，也可能写出整个页
**关键结论**：
- 典型负载下，**LSM树写放大通常更低**（因为不需要写整个页，且可压缩块）
- 写放大直接影响：写入吞吐量（瓶颈在磁盘带宽时） + SSD磨损速度
- 测试时需运行足够长时间：空LSM树无压实，写入很快；随着数据增长，压实会与写入竞争磁盘带宽
#### 磁盘空间使用
**B树的磁盘空间问题**：
- 删除大量键后，页变得空闲但无法归还给操作系统（在文件中间）
- 产生**碎片化**
- 需要后台进程（如PostgreSQL的vacuum）重排页
**LSM树的磁盘空间优势**：
- 压实过程定期重写数据文件，碎片化问题小
- SSTable没有未使用的页
- 键值对块可压缩，文件通常比B树更小
**LSM树的磁盘空间代价**：
- 被覆盖的键和值直到压实才删除（分级压实开销低，分层压实开销大）
- 数据可能有多份副本
- 删除（墓碑）需要经过所有压实级别才能彻底清除，可能耗时很长
**LSM树的独特优势**：
- 段文件不可变，便于创建快照（备份/测试）
- 只需记录内存表和段文件列表，无需复制数据
- B树页被覆盖后，快照困难得多
### 多列索引与二级索引
**二级索引**是按主键以外的列建立的索引，用于快速查找非主键字段。与主键索引的主要区别在于：二级索引的值不一定唯一。
**构建方式**：
- 本质与键值索引相同，键为索引列的值，值为匹配行的标识符
- 处理非唯一值的两种方法：
    1. **值设为列表**：每个索引条目存储匹配行ID的列表（如倒排索引）
    2. **追加行ID**：给每个索引条目追加行标识符，使其唯一
#### 在索引中存储值
索引中的**值**可以存储三种不同类型的内容：
**1. 聚簇索引**（例：MySQL InnoDB的主键索引）
- 实际数据直接存储在索引结构中
- 数据随索引一起存放，无需二次查找
**2. 非聚簇索引（堆文件方式）**（例：PostgreSQL）
- 索引存储的是**数据的引用**（主键或磁盘位置）
- 实际数据存在独立的堆文件中，顺序任意
- 查询需两次查找：先查索引得引用，再查堆文件取数据
**3. 覆盖索引（折中方案）**
- 索引中存储表的**部分列**，其余列仍存别处
- 某些查询可仅用索引完成（称为**索引覆盖**），无需访问堆文件
- 代价：数据重复，占用更多磁盘空间，写入变慢
**更新时的差异**：
- 堆文件方式：新值≤旧值（指的是**占用空间**）可原地覆盖；新值更大需移动位置，索引需更新或留转发指针
- 聚簇索引：数据随索引移动，复杂度更高
到目前为止讨论的索引只将单个键映射到值。如果你需要同时查询表的多个列（或文档中的多个字段），请参见 [“多维索引与全文索引”](https://ddia.vonng.com/ch4/#sec_storage_multidimensional)。
### 全内存存储
**内存数据库**分两类：一类像Memcached，只做缓存，重启丢数据也无所谓；另一类追求持久化，靠写磁盘日志、定期快照或数据复制来保证重启后能恢复。
有趣的是，内存数据库快的原因不是“不用读磁盘”——磁盘数据库有足够内存时，操作系统也会把数据缓存在RAM里。真正的优势是省去了把**内存数据结构**编码成磁盘格式的开销。
除了性能，内存数据库还能轻松支持一些磁盘索引很难实现的数据模型。比如Redis提供了**队列、集合**等丰富的数据结构，实现起来简单得多。
## 分析型数据存储
### 云数据仓库
- 云数据仓库通过“存算分离”架构和组件解耦（查询引擎、存储格式、表格式、数据目录），实现了比传统系统更强的弹性、集成能力和开放性。
- 存算分离
	- **核心机制**：将**查询计算**与**存储层解耦**。
	- **实现方式**：数据持久化在**对象存储**（而非本地磁盘），使**存储容量**和**计算资源**可独立伸缩。
- 解耦后的四大独立组件

|组件|功能描述|代表技术/产品|
|---|---|---|
|**查询引擎**|解析SQL、优化为执行计划、并行/分布式执行查询|Trino, Presto, Apache DataFusion (可搭配 Spark/Flink 执行)|
|**存储格式**|定义行数据如何编码为文件字节，供查询引擎及多种应用访问|Parquet, ORC, Lance, Nimble|
|**表格式**|管理不可变文件，支持行级插入/删除、时间旅行、事务等高级功能|Apache Iceberg, Databricks Delta|
|**数据目录**|定义哪些表组成数据库，管理元数据，支持数据发现与治理|Snowflake Polaris, Databricks Unity Catalog, Iceberg目录 (常作为独立REST服务)|
### 列式存储
- 列式存储通过将同一列的数据连续存放，让分析查询只读取所需的列，从而显著降低磁盘I/O，提升海量数据（如PB级事实表）的查询性能。
- **基本概念**
    - **与传统行式存储相反**：行式存储（用于OLTP数据库）把同一行的所有值挨着存放；而列式存储把**每一列的所有值**单独存放在一起。
    - **相同顺序**：所有列都按照相同的顺序存储行，因此可以通过位置（例如每列的第23个条目）重新组装出完整的行。
- **主要优势（针对分析查询）**
	- 示例4-1：它访问大量行（2024 日历年期间每次有人购买水果或糖果的情况），但它只需要访问 `fact_sales` 表的三列：`date_key`、`product_sk` 和 `quantity`。查询忽略所有其他列。
```sql
SELECT
    dim_date.weekday, dim_product.category,
    SUM(fact_sales.quantity) AS quantity_sold
FROM fact_sales
    JOIN dim_date ON fact_sales.date_key = dim_date.date_key
    JOIN dim_product ON fact_sales.product_sk = dim_product.product_sk
WHERE
    dim_date.year = 2024 AND
    dim_product.category IN ('Fresh fruit', 'Candy')
GROUP BY
    dim_date.weekday, dim_product.category;
```
  **大幅减少I/O**：分析查询（如示例4-1）通常只访问事实表中上百列里的少数几列（如4-5列）。列式存储只需读取查询涉及的列，避免了将整行所有列的数据都从磁盘加载到内存，从而节省大量磁盘读取和解析时间。
    - **提高效率**：对于“扫描大量行但只取少量列”的典型数据仓库查询，列式存储极为高效。
- **实际实现方式**
    - 列存储引擎不会把整列（可能数万亿行）当作一个巨大文件，而是将表分解成**数千或数百万行的块（block）**，如使用 [图 3-5](https://ddia.vonng.com/ch3/#fig_dwh_schema) 中事实表的扩展版本[图 4-7](https://ddia.vonng.com/ch4/#fig_column_store)
    - ![](https://ddia.vonng.com/fig/ddia_0407.png)

	图 4-7. 按列而不是按行存储关系数据。
    - 在每个块内部，分别存储每一列的值。
    - 通常按时间戳范围划分块，这样查询时**只需加载与所需日期范围重叠的块中必要的列**。
- **应用范围**
    - **主流分析数据库**：几乎用于所有分析型数据库，包括云数据仓库（Snowflake）、单节点嵌入式数据库（DuckDB）、产品分析系统（Pinot、Druid）。
    - **存储格式**：Parquet、ORC、Lance、Nimble。
    - **内存分析格式**：Apache Arrow、Pandas/NumPy。
    - **时间序列数据库**：InfluxDB IOx、TimescaleDB 等也基于列式存储。
#### 列压缩
- **列式存储为何适合压缩**
    - 同一列的数据连续存放在一起，数据仓库中一列的不同值数量通常远小于总行数（例如几十亿条销售记录但只有十万种产品）
    - 这种特点导致列数据中出现大量重复值，为压缩创造了很好的条件
- **位图编码（特别有效的压缩技术）**
    - **构建位图**：把一个有 n 个不同值的列拆成 n 个独立的位图，每个位图对应一个值，长度等于表的行数。如果某一行取这个值，对应位设为 1，否则为 0，如 [图 4-8](https://ddia.vonng.com/ch4/#fig_bitmap_index) 所示。

![](https://ddia.vonng.com/fig/ddia_0408.png)
    图 4-8. 单列的压缩、位图索引存储。
	- **进一步压缩**：位图通常很稀疏（包含大量 0），可以用游程编码压缩——不存储每个单独的 0 或 1，而是记录连续 0 或 1 的个数。更高级的做法如咆哮位图会在不同表示方法间自动切换，始终选择最紧凑的一种
- **压缩后的位图可直接作为索引**
    - **IN 条件查询**：例如 `WHERE product_sk IN (31, 68, 69)`，加载三个位图并执行按位 OR 操作
    - **AND 条件查询**：例如 `WHERE product_sk = 30 AND store_sk = 3`，加载两个位图并执行按位 AND 操作。由于各列的行顺序一致，不同位图中第 k 位对应的是同一行
    - **图查询**：也可用于查找满足特定关系条件的用户（例如被用户 X 关注同时也关注用户 Y 的所有用户）
#### 列存储中的排序顺序
**排序的基本前提**
- 在列存储中，独立地对每一列排序是没有意义的，因为那样会丢失列之间的对应关系——重建一行依赖于知道所有列中第k个条目属于同一行
- 正确的做法是：**一次性地对整个表确定行的顺序**（即决定所有列共同的排列顺序），然后按这个顺序分别存储每一列的内容
**如何选择排序键**
- 数据库管理员可以根据常见查询来选择表应按哪些列排序
- **第一排序键示例**：如果查询经常针对日期范围（例如“上个月”），将 [图 4-7](https://ddia.vonng.com/ch4/#fig_column_store)中`date_key`作为第一个排序键是有意义的。这样查询可以只扫描上个月对应的那些行块，比扫描全表快得多
- **后续排序键示例**：第二列用于确定第一列中相同值内部的顺序。例如`date_key`相同的情况下，按`product_sk`排序，可以让同一天同一产品的所有销售在存储中聚集在一起，有助于按产品分组或过滤的查询
**排序对压缩的好处**
- 排序可以显著提升压缩效果。如果排序列没有太多不同值，排序后该列会出现很长的连续重复值序列
- **游程编码的效果**：简单的游程编码（记录连续相同值的个数）就可以将这样的列压缩到几千字节，即使表有数十亿行
- **不同优先级列的差异**：第一排序键的压缩效果最强（重复值序列最长），第二、第三排序键的重复模式会逐渐混乱，压缩效果依次减弱。排序优先级越低的列，值基本上以随机顺序出现，压缩效果最差。但让前几列排序仍然是整体上的净收益
#### 写入列式存储
- 使用列式存储，在排序表的中间某处写入单个行将非常低效，因为你必须从插入位置开始重写所有压缩列。但是，一次批量写入许多行会分摊重写这些列的成本，使其高效。
- 通常使用日志结构方法以批次执行写入。所有写入首先进入面向行的、排序的内存存储。当积累了足够的写入时，它们将与磁盘上的列编码文件合并，并批量写入新文件。由于旧文件保持不可变，新文件一次写入，对象存储非常适合存储这些文件。
### 查询执行：编译与向量化
**查询执行的两大挑战**
- 分析查询不仅需要从磁盘读取大量数据，执行查询本身的CPU时间也可能成为瓶颈
- 最简单的“解释执行”方式（逐行判断要做什么操作）对大多数分析场景来说太慢了
**解决方案一：查询编译**
- 查询引擎将SQL直接生成对应的执行代码，然后编译成机器码（类似JVM的JIT编译）
- 编译后的代码逐行处理列中的值，只做必要的比较和计算，效率很高
**解决方案二：向量化处理**
- 不编译整个查询，而是用数据库内置的一组固定算子，每个算子批量处理一批值
- 例如：相等算子输入一列和一个目标值，输出一个位图；然后按位AND算子将两个位图合并。整个过程批量进行，而不是逐行循环。我们可以将 `product_sk` 列和"香蕉"的 ID 传递给相等算子，并获得一个位图（输入列中每个值一位，如果是香蕉则为 1）；然后我们可以将 `store_sk` 列和感兴趣商店的 ID 传递给相同的相等算子，并获得另一个位图；然后我们可以将两个位图传递给"按位 AND"算子，如 [图 4-9](https://ddia.vonng.com/ch4/#fig_bitmap_and) 所示。结果将是一个位图，包含特定商店中所有香蕉销售的 1。

![](https://ddia.vonng.com/fig/ddia_0409.png)

图 4-9. 两个位图之间的按位 AND 适合向量化。
**两者的共同优化手段**
- 优先顺序访问内存，减少随机访问带来的缓存未命中
- 内部循环保持简单（指令少、无函数调用），让CPU流水线更顺畅
- 利用多线程和SIMD指令并行处理
- 直接对压缩数据操作，避免先解压带来的额外内存开销
### 物化视图与数据立方体
**物化视图 vs. 虚拟视图**
- 虚拟视图只是查询的快捷方式，读取时SQL引擎会即时将其展开为底层查询
- 物化视图则是查询结果的实际副本，写入磁盘。当基础数据变化时需要更新（有些数据库或系统如Materialize可自动维护），写入代价更高，但能显著提升重复查询的读取性能
**数据立方体（OLAP立方体）**
- 是物化聚合的一种形式，适用于经常按不同维度做分组聚合（COUNT、SUM、AVG等）的查询
- 原理：创建按各个维度组合预先计算好的聚合网格。比如两个维度（日期、产品）可形成一个二维表，每个单元格是特定日期-产品组合的聚合值，再沿行或列汇总得到更高层次的摘要
- 扩展到多个维度（如五维超立方体）原理相同：每个单元格对应一个完整维度组合的聚合，可沿任一维度向上汇总
- 现在假设每个事实只有两个维度表的外键 —— 在 [图 4-10](https://ddia.vonng.com/ch4/#fig_data_cube) 中，这些是 `date_key` 和 `product_sk`。你现在可以绘制一个二维表，日期沿着一个轴，产品沿着另一个轴。每个单元格包含具有该日期-产品组合的所有事实的属性（例如 `net_price`）的聚合（例如 `SUM`）。然后，你可以沿着每行或列应用相同的聚合，并获得已减少一个维度的摘要（不管日期的产品销售，或不管产品的日期销售）。
![](https://ddia.vonng.com/fig/ddia_0410.png)

图 4-10. 数据立方体的两个维度，通过求和聚合数据。
**优缺点**
- 优点：某些查询变得非常快，因为结果已预计算好，无需扫描原始数据
- 缺点：灵活性不如直接查询原始数据。例如无法计算“售价超过100美元的商品销售占比”，因为价格不是预定义的维度之一
- 实际做法：大多数数据仓库会保留原始数据，只把数据立方体这类聚合当作特定查询的性能加速手段
## 多维索引与全文索引
**联合索引（多列索引的基础形式）**
- 通过将几列按顺序拼接成一个组合键来工作，例如电话簿按（姓氏，名字）排序
- 优点：可以高效查找特定姓氏，或特定（姓氏+名字）组合
- 局限：无法高效查找只有名字的人，因为名字不是索引的第一列
**多维索引**
- 允许同时查询多个列，解决联合索引无法处理多维度同时筛选的问题。例如，餐厅搜索网站可能有一个包含每个餐厅的纬度和经度的数据库。当用户在地图上查看餐厅时，网站需要搜索用户当前查看的矩形地图区域内的所有餐厅。这需要像以下这样的二维范围查询：

```sql
SELECT * FROM restaurants WHERE latitude > 51.4946 AND latitude < 51.5079
    AND longitude > -0.1162 AND longitude < -0.1004;
```

纬度和经度列上的联合索引无法有效地回答这种查询：它可以为你提供纬度范围内的所有餐厅（但在任何经度），或经度范围内的所有餐厅（但在北极和南极之间的任何地方），但不能同时提供两者。
- 典型场景是地理空间数据，例如搜索矩形地图区域内的餐厅，需要同时满足纬度和经度的范围条件
- 实现方式包括：用空间填充曲线将二维位置转成一维数字后使用B树，或使用专门的空间索引如R树、Bkd树（PostGIS就是基于R树实现），或使用规则网格（三角形、正方形、六边形）
- 其他应用例子：颜色空间的三维索引（红绿蓝范围搜索）、天气数据的二维索引（日期+温度），可以同时按两个维度缩小范围，避免扫描其中一个维度的全部数据
### 全文检索
略
### 向量嵌入
略
## 总结
在本章中，我们试图深入了解数据库如何执行存储和检索。当你在数据库中存储数据时会发生什么，当你稍后再次查询数据时数据库会做什么？
[“分析型与事务型系统”](https://ddia.vonng.com/ch1/#sec_introduction_analytics) 介绍了事务处理（OLTP）和分析（OLAP）之间的区别。在本章中，我们看到为 OLTP 优化的存储引擎与为分析优化的存储引擎看起来非常不同：
- OLTP 系统针对大量请求进行了优化，每个请求读取和写入少量记录，并且需要快速响应。记录通常通过主键或二级索引访问，这些索引通常是从键到记录的有序映射，也支持范围查询。
- 数据仓库和类似的分析系统针对扫描大量记录的复杂读取查询进行了优化。它们通常使用带有压缩的列式存储布局，以最小化此类查询需要从磁盘读取的数据量，并使用查询的即时编译或向量化来最小化处理数据所花费的 CPU 时间。
在 OLTP 方面，我们看到了两个主要思想流派的存储引擎：
- 日志结构方法，只允许追加到文件和删除过时文件，但从不更新已写入的文件。SSTable、LSM 树、RocksDB、Cassandra、HBase、Scylla、Lucene 等属于这一组。一般来说，日志结构存储引擎往往提供高写入吞吐量。
- 就地更新方法，将磁盘视为一组可以覆盖的固定大小页。B 树是这种理念的最大例子，用于所有主要的关系型 OLTP 数据库以及许多非关系型数据库。作为经验法则，B 树往往更适合读取，提供比日志结构存储更高的读取吞吐量和更低的响应时间。
然后我们查看了可以同时搜索多个条件的索引：多维索引（如 R 树）可以同时按纬度和经度搜索地图上的点，全文检索索引可以搜索出现在同一文本中的多个关键字。最后，向量数据库用于文本文档和其他媒体的语义搜索；它们使用具有大量维度的向量，并通过比较向量相似性来查找相似文档。