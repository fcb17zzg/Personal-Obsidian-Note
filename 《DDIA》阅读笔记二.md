# 4. 存储与检索
- 在最基础的层面上，数据库需要做两件事：当你给它一些数据时，它应该存储这些数据；当你之后再询问时，它应该把数据返回给你。在本章中，我们从数据库的角度讨论同样的问题：数据库如何存储你提供的数据，以及当你请求时如何再次找到这些数据。并且考虑不同存储请求方式的优化。
- 本章首先研究两种用于 OLTP 的存储引擎家族：写入不可变数据文件的 _日志结构_ 存储引擎，以及像 _B 树_ 这样就地更新数据的存储引擎。这些结构既用于键值存储，也用于二级索引。随后在 [“分析型数据存储”](https://ddia.vonng.com/ch4/#sec_storage_analytics) 中，我们将讨论一系列针对分析优化的存储引擎；在 [“多维索引与全文索引”](https://ddia.vonng.com/ch4/#sec_storage_multidimensional) 中，我们将简要介绍用于更高级查询（如文本检索）的索引。
## OLTP 系统的存储与索引

| 概念          | 说明                         |
| ----------- | -------------------------- |
| **OLTP**    | 在线事务处理，面向大量小规模读写（如用户注册、下单） |
| **日志（Log）** | 仅追加的磁盘文件，写入高效              |
| **索引**      | 从主数据派生的额外结构，用于加速读取         |

- 最简单的存储：两个Bash函数

```bash
db_set () {
  echo "$1,$2" >> database   # 追加到文件末尾
}

db_get () {
  grep "^$1," database | sed -e "s/^$1,//" | tail -n 1  # 扫描整个文件
}
```

**使用示例**：

```bash
$ db_set 12 '{"name":"London","attractions":["Big Ben","London Eye"]}'
$ db_set 42 '{"name":"San Francisco","attractions":["Golden Gate Bridge"]}'
$ db_get 42
{"name":"San Francisco","attractions":["Golden Gate Bridge"]}
```

**多次更新同一个key**：

```bash
$ db_set 42 '{"name":"San Francisco","attractions":["Exploratorium"]}'
$ db_get 42
{"name":"San Francisco","attractions":["Exploratorium"]}

$ cat database
12,{"name":"London","attractions":["Big Ben","London Eye"]}
42,{"name":"San Francisco","attractions":["Golden Gate Bridge"]}
42,{"name":"San Francisco","attractions":["Exploratorium"]}
```

- 存储：日志（Log）
	- **原理**：每次写入直接追加到文件末尾
	- **优点**：写入非常高效（O(1)）
	- **真实数据库**：同样使用仅追加的数据文件作为日志
	- **额外处理**：并发写入、空间回收、崩溃恢复
> 注：日志不一定是人类可读的文本，可以是二进制的。
-  **索引的作用**：用额外结构（如排序、哈希）加速查找。
-  索引的代价

| 代价类型 | 说明 |
|----------|------|
| **写入变慢** | 每次写入不仅要写数据，还要更新索引 |
| **占用空间** | 索引消耗额外磁盘空间 |
| **需要手动选择** | 数据库不会自动为所有字段建索引，由开发者根据查询模式选择 |

- 权衡
> 	**索引加快读查询，但减慢写入速度。**
	- 日志（仅追加）是最快的写入方式
	- 任何索引都会破坏这种"纯粹"的写入性能
	- 需要根据应用的查询模式，选择收益最大的索引
- 总结：**日志（仅追加文件）= 写入快。索引 = 读取快，但写入变慢且占空间。OLTP系统需要你在两者之间做权衡。**
### 日志结构存储
在仅追加的日志文件中存储键值对时，可以在内存中维护一个哈希映射，记录每个键最新值的字节偏移量，从而加快读取速度。写入时更新映射，读取时通过映射定位并读取值（若数据在缓存中则无需磁盘I/O）。
如 [图 4-1](https://ddia.vonng.com/ch4/#fig_storage_csv_hash_index) 所示。

![](https://ddia.vonng.com/fig/ddia_0401.png)

图 4-1. 以类似 CSV 格式存储键值对日志，使用内存哈希映射建立索引。
但该方法存在以下问题：
- **磁盘空间无法释放**：旧日志条目不会被清理，长期写入可能耗尽空间。
- **重启慢**：哈希映射不持久化，重启需扫描整个日志文件重建索引。
- **内存限制**：哈希表必须存于内存，磁盘哈希表因随机I/O、扩容成本高和冲突处理复杂而难以高效实现。
- **范围查询效率低**：无法高效扫描键范围，只能逐个查找。
#### SSTable 文件格式
**SSTable（排序字符串表）**是一种按键排序存储键值对的文件格式，每个键在文件中只出现一次。
其核心特点是：
- 将键值对按顺序分组为若干**块**（如几千字节）
- 在内存中维护一个**稀疏索引**，只记录每个块的第一个键
- 索引可使用 B 树、字典树等结构单独存储
**查找过程**（以键 `handiwork` 为例）：
1. 在稀疏索引中定位到相邻的两个键：`handbag` 和 `handsome`
2. 根据排序规则，`handiwork` 应位于二者之间
3. 寻址到 `handbag` 所在的块偏移量
4. 从该位置开始顺序扫描，直到找到目标键或确认不存在
![](https://ddia.vonng.com/fig/ddia_0402.png)

图 4-2. 带有稀疏索引的 SSTable，允许查询跳转到正确的块。
**优点**：
- 内存中只需保存稀疏索引，大幅降低内存占用
- 每个数据块可压缩，节省磁盘空间并减少 I/O 带宽（以少量 CPU 为代价）
- 范围查询高效，支持快速跳转
#### 构建和合并 SSTable
**内存表 + 磁盘SSTable段 + 后台合并**。
- 1. 写入与构建
	- **内存表**：写入操作先进入内存中的有序结构（如红黑树、跳表），支持任意顺序插入。
	- **持久化日志**：同时将写入追加到磁盘日志，用于崩溃后恢复内存表。
	- **刷盘**：内存表达到阈值后，将其排序后作为SSTable文件写入磁盘（成为新“段”），随后释放内存并丢弃对应日志。
-  2. 读取
	按顺序查找：**内存表 → 最新磁盘段 → 较旧段**，直到找到键或确认不存在。
- 3. 合并与压实（后台进行）
	- 类似归并排序：同时读取多个段文件，每次取最小键写入新段。
	- 同一键出现多次时，**只保留最新值**。
	- 删除通过写入“墓碑”记录实现，合并时丢弃已删除键的旧值。
	- 如 [图 4-3](https://ddia.vonng.com/ch4/#fig_storage_sstable_merging) 所示：并排开始读取输入文件，查看每个文件中的第一个键，将最低的键（根据排序顺序）复制到输出文件，然后重复。如果同一个键出现在多个输入文件中，只保留较新的值。这会产生一个新的合并段文件，也按键排序，每个键只有一个值，并且它使用最少的内存，因为我们可以一次遍历一个键的 SSTable。

![](https://ddia.vonng.com/fig/ddia_0403.png)

图 4-3. 合并多个 SSTable 段，仅保留每个键的最新值。
- 4. 关键优势
	- **段文件不可变**：简化崩溃恢复（未完成的SSTable可直接丢弃）。
	- **支持对象存储**：不限于本地磁盘（如SlateDB、Delta Lake）。
	- **成熟应用**：RocksDB、Cassandra、HBase等均采用此算法。
#### 布隆过滤器
工作原理
- **构建**：将 SSTable 中每个键通过哈希函数映射到位数组的多个位置，并将这些位设为 1。
- **查询**：对目标键计算相同哈希，检查对应位：
    - **至少一位为 0** → 键**肯定不在**该 SSTable 中（可安全跳过）
    - **所有位均为 1** → 键**很可能存在**（可能出现**假阳性**）
- [图 4-4] 显示了一个包含两个键和 16 位的布隆过滤器示例（实际上，它会包含更多的键和更多的位）。对于 SSTable 中的每个键，我们计算一个哈希函数，产生一组数字，然后将其解释为位数组的索引 [14](https://ddia.vonng.com/ch4/#fn:14)。我们将对应于这些索引的位设置为 1，其余保持为 0。例如，键 `handbag` 哈希为数字 (2, 9, 4)，所以我们将第 2、9 和 4 位设置为 1。然后将位图与键的稀疏索引一起存储为 SSTable 的一部分。这需要一点额外的空间，但与 SSTable 的其余部分相比，布隆过滤器通常很小。

![](https://ddia.vonng.com/fig/ddia_0404.png)

图 4-4. 布隆过滤器提供了一种快速的概率检查，用于判断特定键是否存在于特定 SSTable 中。
 关键特性
- **假阳性**：所有位为 1 但键实际不存在（由其他键的位巧合造成）
- **无假阴性**：若布隆过滤器说“不存在”，则 100% 不存在
- **空间与精度权衡**：每个键约 10 位 → 1% 假阳性率；每增加 5 位/键 → 假阳性率降低 10 倍
 在 LSM 中的价值
- **跳过无关段**：快速排除不包含该键的 SSTable，减少不必要的磁盘扫描
- **假阳性无害**：仅造成少量额外工作，继续查找下一段即可
布隆过滤器极小（相对 SSTable），利用位运算可极快判断，是 LSM 树读取优化的重要手段。
#### 压实策略
1. 分层压实（Size-tiered Compaction）
**工作机制**：
- 较小、较新的SSTable逐步合并成较大、较旧的SSTable
- 类似大小的段被合并成一个更大的段
**特点**：
- **优点**：能处理非常高的写入吞吐量
- **缺点**：旧数据段可能变得非常大，合并时需要大量临时磁盘空间
2. 分级压实（Leveled Compaction）
**工作机制**：
- 将键范围分成多个较小的SSTable  
- 旧数据被移动到独立的"级别"（levels）中
- 以更增量的方式进行压实
**特点**：
- **优点**：比分层策略使用更少的磁盘空间，读取更高效（需检查的SSTable更少）
- **缺点**：写入放大相对较高
**打个比方：**
- **分层压实**：像是用**卡车**拉货。等货物攒够一整车再出发。虽然车次少，但每趟拉得多，**运输效率（写入吞吐量）**很高。
- **分级压实**：像是**频繁发车的小面包车**。每来一点货就立刻送走。车次频繁，对**查询（读取）** 友好，但运输成本高，拉新货的效率不如卡车。
策略选择建议

| 工作负载       | 推荐策略 | 原因        |
| ---------- | ---- | --------- |
| **写多读少**   | 分层压实 | 写入吞吐量更高   |
| **读多写少**   | 分级压实 | 读取效率更好    |
| **频繁写少量键** | 分级压实 | 避免大量小文件合并 |
| **批量写大量键** | 分层压实 | 合并效率高     |
 **核心思想**：无论选择哪种策略，LSM树本质都是在后台维护不断合并的SSTable级联结构，实现高效的写入和可接受的读取性能。
### B 树
我们之前看到的日志结构索引将数据库分解为可变大小的 _段_，通常为几兆字节或更大，写入一次后就不可变。相比之下，B 树将数据库分解为固定大小的 _块_ 或 _页_，并可能就地覆盖页。页传统上大小为 4 KiB，但 PostgreSQL 现在默认使用 8 KiB，MySQL 默认使用 16 KiB。

每个页都可以使用页号来标识，这允许一个页引用另一个页 —— 类似于指针，但在磁盘上而不是在内存中。如果所有页都存储在同一个文件中，将页号乘以页大小就给我们文件中页所在位置的字节偏移量。我们可以使用这些页引用来构建页树，如 [图 4-5](https://ddia.vonng.com/ch4/#fig_storage_b_tree) 所示。

![](https://ddia.vonng.com/fig/ddia_0405.png)

图 4-5. 使用 B 树索引查找键 251。从根页开始，我们首先跟随引用到键 200–300 的页，然后是键 250–270 的页。

一个页被指定为 B 树的 _根_；每当你想在索引中查找一个键时，你就从这里开始。该页包含几个键和对子页的引用。每个子页负责一个连续的键范围，引用之间的键指示这些范围之间的边界在哪里。（这种结构有时称为 B+ 树，但我们不需要将其与其他 B 树变体区分开来。）

在 [图 4-5](https://ddia.vonng.com/ch4/#fig_storage_b_tree) 的例子中，我们正在查找键 251，所以我们知道我们需要跟随边界 200 和 300 之间的页引用。这将我们带到一个看起来相似的页，该页进一步将 200–300 范围分解为子范围。最终我们到达包含单个键的页（_叶页_），该页要么内联包含每个键的值，要么包含对可以找到值的页的引用。

B 树的一个页中对子页的引用数称为 _分支因子_。例如，在 [图 4-5](https://ddia.vonng.com/ch4/#fig_storage_b_tree) 中，分支因子为六。实际上，分支因子取决于存储页引用和范围边界所需的空间量，但通常为几百。

如果你想更新 B 树中现有键的值，你搜索包含该键的叶页，并用包含新值的版本覆盖磁盘上的该页。如果你想添加一个新键，你需要找到其范围包含新键的页并将其添加到该页。如果页中没有足够的空闲空间来容纳新键，则页被分成两个半满的页，并更新父页以说明键范围的新细分。

![](https://ddia.vonng.com/fig/ddia_0406.png)

图 4-6. 通过在边界键 337 上分割页来增长 B 树。父页被更新以引用两个子页。

在 [图 4-6](https://ddia.vonng.com/ch4/#fig_storage_b_tree_split) 的例子中，我们想插入键 334，但范围 333–345 的页已经满了。因此，我们将其分成范围 333–337（包括新键）的页和 337–344 的页。我们还必须更新父页以引用两个子页，它们之间的边界值为 337。如果父页没有足够的空间容纳新引用，它也可能需要被分割，分割可以一直持续到树的根。当根被分割时，我们在它上面创建一个新根。删除键（可能需要合并节点）更复杂 [5](https://ddia.vonng.com/ch4/#fn:5)。

这个算法确保树保持 _平衡_：具有 _n_ 个键的 B 树始终具有 _O_(log _n_) 的深度。大多数数据库可以适合三或四层深的 B 树，所以你不需要跟随许多页引用来找到你要查找的页。（具有 500 分支因子的 4 KiB 页的四层树可以存储多达 250 TB。）
#### 使 B 树可靠
**核心问题**：B树直接覆盖磁盘上的页（原地修改）。如果在写入过程中崩溃，可能造成数据损坏（如撕裂页、孤立页）。
**解决方案：预写日志（WAL）**
- 修改B树之前，先将所有操作写入一个仅追加的日志文件
- 日志写入磁盘后，才能修改B树页
- 崩溃恢复时，重放日志即可恢复到一致状态。在文件系统中，等效机制称为 _日志记录_（_journaling_）。
**内存缓冲 + WAL**
- B树页先在内存中缓存，不立即写盘
- 但WAL必须用`fsync()`强制刷盘
- 确保即使崩溃，日志中的操作也不会丢失
**对比LSM**：LSM只追加文件、不原地修改，天然更易恢复；B树依赖WAL实现原子性和持久性。
#### B 树变体
**写时复制（如LMDB）**
- 不覆盖原有页，也不使用WAL
- 修改的页写入新位置，父页更新指向新位置
- 好处：崩溃恢复简单，也利于实现快照隔离
**键缩写**
- 内部页不存完整键，只存足够区分范围的边界信息
- 每页能塞更多键 → 分支因子更高 → 树层数更少
**叶页顺序布局**
- 让叶页在磁盘上物理连续排列
- 好处：范围扫描时减少磁盘寻道
- 难点：树增长后很难维持这种顺序
**横向指针（B+树）***
- 叶页增加指向左右兄弟页的指针
- 顺序扫描时无需跳回父页，直接横向遍历
### 比较 B 树与 LSM 树
作为经验法则，LSM 树更适合写入密集型应用，而 B 树对读取更快。存储引擎有时会混合两种方法的特征，例如具有多个 B 树并以 LSM 风格合并它们。
#### 读取性能
**点查询**
- **B树**：层级少，每层读一页，性能稳定可预测
- **LSM**：需检查多个SSTable，但布隆过滤器可跳过无关段，减少磁盘I/O
**范围查询**
- **B树**：利用树的排序结构，简单快速
- **LSM**：需并行扫描所有段并合并结果，布隆过滤器无法帮助范围查询，因此比点查询昂贵得多
**写入压力影响**
- LSM在高写入吞吐量下，若压实跟不上写入速度，内存表填满会导致延迟峰值
- 许多存储引擎（如RocksDB）会执行**背压**：暂停读写直到内存表刷盘完成
**并行读取**
- 现代SSD（特别是NVMe）支持大量并行读请求
- 两种引擎都能提供高读取吞吐量，但需要精心设计以利用并行性
#### 顺序与随机写入
**B树：随机写入**
- 键分散在整个空间，需要覆盖不同位置的页
- 每次写入都是小规模、分散的磁盘操作
- 写入吞吐量较低
**LSM树：顺序写入**
- 写入整段文件（内存表刷盘或压实合并）
- 每次写入规模较大、连续
- 写入吞吐量较高
**硬件差异**
- **HDD**：顺序 vs 随机差距巨大（机械寻道需几毫秒）
- **SSD**：差距缩小但仍明显
**SSD上随机写入慢的原因**
- 闪存特性：读/写单位是页（4KB），擦除单位是块（512KB）
- 随机写入导致块内混合有效和无效数据
- 擦除前需搬移有效数据（垃圾回收 GC）
- GC消耗带宽，增加写放大，加速闪存磨损
#### 写放大
**写放大**指数据库执行一次应用写入时，底层磁盘实际发生的I/O操作倍数。计算方式：实际写入磁盘的总字节数 ÷ 仅追加日志的写入字节数。
**LSM树的写放大来源**：
- 先写日志（持久性）
- 内存表刷盘时再写一次
- 每次压实又会重写数据
**B树的写放大来源**：
- 写预写日志（WAL）一次
- 写树页一次
- 即使只改几个字节，也可能写出整个页
**关键结论**：
- 典型负载下，**LSM树写放大通常更低**（因为不需要写整个页，且可压缩块）
- 写放大直接影响：写入吞吐量（瓶颈在磁盘带宽时） + SSD磨损速度
- 测试时需运行足够长时间：空LSM树无压实，写入很快；随着数据增长，压实会与写入竞争磁盘带宽
#### 磁盘空间使用
**B树的磁盘空间问题**：
- 删除大量键后，页变得空闲但无法归还给操作系统（在文件中间）
- 产生**碎片化**
- 需要后台进程（如PostgreSQL的vacuum）重排页
**LSM树的磁盘空间优势**：
- 压实过程定期重写数据文件，碎片化问题小
- SSTable没有未使用的页
- 键值对块可压缩，文件通常比B树更小
**LSM树的磁盘空间代价**：
- 被覆盖的键和值直到压实才删除（分级压实开销低，分层压实开销大）
- 数据可能有多份副本
- 删除（墓碑）需要经过所有压实级别才能彻底清除，可能耗时很长
**LSM树的独特优势**：
- 段文件不可变，便于创建快照（备份/测试）
- 只需记录内存表和段文件列表，无需复制数据
- B树页被覆盖后，快照困难得多
### 多列索引与二级索引
**二级索引**是按主键以外的列建立的索引，用于快速查找非主键字段。与主键索引的主要区别在于：二级索引的值不一定唯一。
**构建方式**：
- 本质与键值索引相同，键为索引列的值，值为匹配行的标识符
- 处理非唯一值的两种方法：
    1. **值设为列表**：每个索引条目存储匹配行ID的列表（如倒排索引）
    2. **追加行ID**：给每个索引条目追加行标识符，使其唯一
#### 在索引中存储值
索引中的**值**可以存储三种不同类型的内容：
**1. 聚簇索引**（例：MySQL InnoDB的主键索引）
- 实际数据直接存储在索引结构中
- 数据随索引一起存放，无需二次查找
**2. 非聚簇索引（堆文件方式）**（例：PostgreSQL）
- 索引存储的是**数据的引用**（主键或磁盘位置）
- 实际数据存在独立的堆文件中，顺序任意
- 查询需两次查找：先查索引得引用，再查堆文件取数据
**3. 覆盖索引（折中方案）**
- 索引中存储表的**部分列**，其余列仍存别处
- 某些查询可仅用索引完成（称为**索引覆盖**），无需访问堆文件
- 代价：数据重复，占用更多磁盘空间，写入变慢
**更新时的差异**：
- 堆文件方式：新值≤旧值（指的是**占用空间**）可原地覆盖；新值更大需移动位置，索引需更新或留转发指针
- 聚簇索引：数据随索引移动，复杂度更高
到目前为止讨论的索引只将单个键映射到值。如果你需要同时查询表的多个列（或文档中的多个字段），请参见 [“多维索引与全文索引”](https://ddia.vonng.com/ch4/#sec_storage_multidimensional)。
### 全内存存储
**内存数据库**分两类：一类像Memcached，只做缓存，重启丢数据也无所谓；另一类追求持久化，靠写磁盘日志、定期快照或数据复制来保证重启后能恢复。
有趣的是，内存数据库快的原因不是“不用读磁盘”——磁盘数据库有足够内存时，操作系统也会把数据缓存在RAM里。真正的优势是省去了把**内存数据结构**编码成磁盘格式的开销。
除了性能，内存数据库还能轻松支持一些磁盘索引很难实现的数据模型。比如Redis提供了**队列、集合**等丰富的数据结构，实现起来简单得多。
## 分析型数据存储
### 云数据仓库
- 云数据仓库通过“存算分离”架构和组件解耦（查询引擎、存储格式、表格式、数据目录），实现了比传统系统更强的弹性、集成能力和开放性。
- 存算分离
	- **核心机制**：将**查询计算**与**存储层解耦**。
	- **实现方式**：数据持久化在**对象存储**（而非本地磁盘），使**存储容量**和**计算资源**可独立伸缩。
- 解耦后的四大独立组件

|组件|功能描述|代表技术/产品|
|---|---|---|
|**查询引擎**|解析SQL、优化为执行计划、并行/分布式执行查询|Trino, Presto, Apache DataFusion (可搭配 Spark/Flink 执行)|
|**存储格式**|定义行数据如何编码为文件字节，供查询引擎及多种应用访问|Parquet, ORC, Lance, Nimble|
|**表格式**|管理不可变文件，支持行级插入/删除、时间旅行、事务等高级功能|Apache Iceberg, Databricks Delta|
|**数据目录**|定义哪些表组成数据库，管理元数据，支持数据发现与治理|Snowflake Polaris, Databricks Unity Catalog, Iceberg目录 (常作为独立REST服务)|
### 列式存储
- 列式存储通过将同一列的数据连续存放，让分析查询只读取所需的列，从而显著降低磁盘I/O，提升海量数据（如PB级事实表）的查询性能。
- **基本概念**
    - **与传统行式存储相反**：行式存储（用于OLTP数据库）把同一行的所有值挨着存放；而列式存储把**每一列的所有值**单独存放在一起。
    - **相同顺序**：所有列都按照相同的顺序存储行，因此可以通过位置（例如每列的第23个条目）重新组装出完整的行。
- **主要优势（针对分析查询）**
	- 示例4-1：它访问大量行（2024 日历年期间每次有人购买水果或糖果的情况），但它只需要访问 `fact_sales` 表的三列：`date_key`、`product_sk` 和 `quantity`。查询忽略所有其他列。
```sql
SELECT
    dim_date.weekday, dim_product.category,
    SUM(fact_sales.quantity) AS quantity_sold
FROM fact_sales
    JOIN dim_date ON fact_sales.date_key = dim_date.date_key
    JOIN dim_product ON fact_sales.product_sk = dim_product.product_sk
WHERE
    dim_date.year = 2024 AND
    dim_product.category IN ('Fresh fruit', 'Candy')
GROUP BY
    dim_date.weekday, dim_product.category;
```
  **大幅减少I/O**：分析查询（如示例4-1）通常只访问事实表中上百列里的少数几列（如4-5列）。列式存储只需读取查询涉及的列，避免了将整行所有列的数据都从磁盘加载到内存，从而节省大量磁盘读取和解析时间。
    - **提高效率**：对于“扫描大量行但只取少量列”的典型数据仓库查询，列式存储极为高效。
- **实际实现方式**
    - 列存储引擎不会把整列（可能数万亿行）当作一个巨大文件，而是将表分解成**数千或数百万行的块（block）**，如使用 [图 3-5](https://ddia.vonng.com/ch3/#fig_dwh_schema) 中事实表的扩展版本[图 4-7](https://ddia.vonng.com/ch4/#fig_column_store)
    - ![](https://ddia.vonng.com/fig/ddia_0407.png)

	图 4-7. 按列而不是按行存储关系数据。
    - 在每个块内部，分别存储每一列的值。
    - 通常按时间戳范围划分块，这样查询时**只需加载与所需日期范围重叠的块中必要的列**。
- **应用范围**
    - **主流分析数据库**：几乎用于所有分析型数据库，包括云数据仓库（Snowflake）、单节点嵌入式数据库（DuckDB）、产品分析系统（Pinot、Druid）。
    - **存储格式**：Parquet、ORC、Lance、Nimble。
    - **内存分析格式**：Apache Arrow、Pandas/NumPy。
    - **时间序列数据库**：InfluxDB IOx、TimescaleDB 等也基于列式存储。
#### 列压缩
- **列式存储为何适合压缩**
    - 同一列的数据连续存放在一起，数据仓库中一列的不同值数量通常远小于总行数（例如几十亿条销售记录但只有十万种产品）
    - 这种特点导致列数据中出现大量重复值，为压缩创造了很好的条件
- **位图编码（特别有效的压缩技术）**
    - **构建位图**：把一个有 n 个不同值的列拆成 n 个独立的位图，每个位图对应一个值，长度等于表的行数。如果某一行取这个值，对应位设为 1，否则为 0，如 [图 4-8](https://ddia.vonng.com/ch4/#fig_bitmap_index) 所示。

![](https://ddia.vonng.com/fig/ddia_0408.png)
    图 4-8. 单列的压缩、位图索引存储。
	- **进一步压缩**：位图通常很稀疏（包含大量 0），可以用游程编码压缩——不存储每个单独的 0 或 1，而是记录连续 0 或 1 的个数。更高级的做法如咆哮位图会在不同表示方法间自动切换，始终选择最紧凑的一种
- **压缩后的位图可直接作为索引**
    - **IN 条件查询**：例如 `WHERE product_sk IN (31, 68, 69)`，加载三个位图并执行按位 OR 操作
    - **AND 条件查询**：例如 `WHERE product_sk = 30 AND store_sk = 3`，加载两个位图并执行按位 AND 操作。由于各列的行顺序一致，不同位图中第 k 位对应的是同一行
    - **图查询**：也可用于查找满足特定关系条件的用户（例如被用户 X 关注同时也关注用户 Y 的所有用户）
#### 列存储中的排序顺序
**排序的基本前提**
- 在列存储中，独立地对每一列排序是没有意义的，因为那样会丢失列之间的对应关系——重建一行依赖于知道所有列中第k个条目属于同一行
- 正确的做法是：**一次性地对整个表确定行的顺序**（即决定所有列共同的排列顺序），然后按这个顺序分别存储每一列的内容
**如何选择排序键**
- 数据库管理员可以根据常见查询来选择表应按哪些列排序
- **第一排序键示例**：如果查询经常针对日期范围（例如“上个月”），将 [图 4-7](https://ddia.vonng.com/ch4/#fig_column_store)中`date_key`作为第一个排序键是有意义的。这样查询可以只扫描上个月对应的那些行块，比扫描全表快得多
- **后续排序键示例**：第二列用于确定第一列中相同值内部的顺序。例如`date_key`相同的情况下，按`product_sk`排序，可以让同一天同一产品的所有销售在存储中聚集在一起，有助于按产品分组或过滤的查询
**排序对压缩的好处**
- 排序可以显著提升压缩效果。如果排序列没有太多不同值，排序后该列会出现很长的连续重复值序列
- **游程编码的效果**：简单的游程编码（记录连续相同值的个数）就可以将这样的列压缩到几千字节，即使表有数十亿行
- **不同优先级列的差异**：第一排序键的压缩效果最强（重复值序列最长），第二、第三排序键的重复模式会逐渐混乱，压缩效果依次减弱。排序优先级越低的列，值基本上以随机顺序出现，压缩效果最差。但让前几列排序仍然是整体上的净收益
#### 写入列式存储
- 使用列式存储，在排序表的中间某处写入单个行将非常低效，因为你必须从插入位置开始重写所有压缩列。但是，一次批量写入许多行会分摊重写这些列的成本，使其高效。
- 通常使用日志结构方法以批次执行写入。所有写入首先进入面向行的、排序的内存存储。当积累了足够的写入时，它们将与磁盘上的列编码文件合并，并批量写入新文件。由于旧文件保持不可变，新文件一次写入，对象存储非常适合存储这些文件。
### 查询执行：编译与向量化
**查询执行的两大挑战**
- 分析查询不仅需要从磁盘读取大量数据，执行查询本身的CPU时间也可能成为瓶颈
- 最简单的“解释执行”方式（逐行判断要做什么操作）对大多数分析场景来说太慢了
**解决方案一：查询编译**
- 查询引擎将SQL直接生成对应的执行代码，然后编译成机器码（类似JVM的JIT编译）
- 编译后的代码逐行处理列中的值，只做必要的比较和计算，效率很高
**解决方案二：向量化处理**
- 不编译整个查询，而是用数据库内置的一组固定算子，每个算子批量处理一批值
- 例如：相等算子输入一列和一个目标值，输出一个位图；然后按位AND算子将两个位图合并。整个过程批量进行，而不是逐行循环。我们可以将 `product_sk` 列和"香蕉"的 ID 传递给相等算子，并获得一个位图（输入列中每个值一位，如果是香蕉则为 1）；然后我们可以将 `store_sk` 列和感兴趣商店的 ID 传递给相同的相等算子，并获得另一个位图；然后我们可以将两个位图传递给"按位 AND"算子，如 [图 4-9](https://ddia.vonng.com/ch4/#fig_bitmap_and) 所示。结果将是一个位图，包含特定商店中所有香蕉销售的 1。

![](https://ddia.vonng.com/fig/ddia_0409.png)

图 4-9. 两个位图之间的按位 AND 适合向量化。
**两者的共同优化手段**
- 优先顺序访问内存，减少随机访问带来的缓存未命中
- 内部循环保持简单（指令少、无函数调用），让CPU流水线更顺畅
- 利用多线程和SIMD指令并行处理
- 直接对压缩数据操作，避免先解压带来的额外内存开销
### 物化视图与数据立方体
**物化视图 vs. 虚拟视图**
- 虚拟视图只是查询的快捷方式，读取时SQL引擎会即时将其展开为底层查询
- 物化视图则是查询结果的实际副本，写入磁盘。当基础数据变化时需要更新（有些数据库或系统如Materialize可自动维护），写入代价更高，但能显著提升重复查询的读取性能
**数据立方体（OLAP立方体）**
- 是物化聚合的一种形式，适用于经常按不同维度做分组聚合（COUNT、SUM、AVG等）的查询
- 原理：创建按各个维度组合预先计算好的聚合网格。比如两个维度（日期、产品）可形成一个二维表，每个单元格是特定日期-产品组合的聚合值，再沿行或列汇总得到更高层次的摘要
- 扩展到多个维度（如五维超立方体）原理相同：每个单元格对应一个完整维度组合的聚合，可沿任一维度向上汇总
- 现在假设每个事实只有两个维度表的外键 —— 在 [图 4-10](https://ddia.vonng.com/ch4/#fig_data_cube) 中，这些是 `date_key` 和 `product_sk`。你现在可以绘制一个二维表，日期沿着一个轴，产品沿着另一个轴。每个单元格包含具有该日期-产品组合的所有事实的属性（例如 `net_price`）的聚合（例如 `SUM`）。然后，你可以沿着每行或列应用相同的聚合，并获得已减少一个维度的摘要（不管日期的产品销售，或不管产品的日期销售）。
![](https://ddia.vonng.com/fig/ddia_0410.png)

图 4-10. 数据立方体的两个维度，通过求和聚合数据。
**优缺点**
- 优点：某些查询变得非常快，因为结果已预计算好，无需扫描原始数据
- 缺点：灵活性不如直接查询原始数据。例如无法计算“售价超过100美元的商品销售占比”，因为价格不是预定义的维度之一
- 实际做法：大多数数据仓库会保留原始数据，只把数据立方体这类聚合当作特定查询的性能加速手段
## 多维索引与全文索引
**联合索引（多列索引的基础形式）**
- 通过将几列按顺序拼接成一个组合键来工作，例如电话簿按（姓氏，名字）排序
- 优点：可以高效查找特定姓氏，或特定（姓氏+名字）组合
- 局限：无法高效查找只有名字的人，因为名字不是索引的第一列
**多维索引**
- 允许同时查询多个列，解决联合索引无法处理多维度同时筛选的问题。例如，餐厅搜索网站可能有一个包含每个餐厅的纬度和经度的数据库。当用户在地图上查看餐厅时，网站需要搜索用户当前查看的矩形地图区域内的所有餐厅。这需要像以下这样的二维范围查询：

```sql
SELECT * FROM restaurants WHERE latitude > 51.4946 AND latitude < 51.5079
    AND longitude > -0.1162 AND longitude < -0.1004;
```

纬度和经度列上的联合索引无法有效地回答这种查询：它可以为你提供纬度范围内的所有餐厅（但在任何经度），或经度范围内的所有餐厅（但在北极和南极之间的任何地方），但不能同时提供两者。
- 典型场景是地理空间数据，例如搜索矩形地图区域内的餐厅，需要同时满足纬度和经度的范围条件
- 实现方式包括：用空间填充曲线将二维位置转成一维数字后使用B树，或使用专门的空间索引如R树、Bkd树（PostGIS就是基于R树实现），或使用规则网格（三角形、正方形、六边形）
- 其他应用例子：颜色空间的三维索引（红绿蓝范围搜索）、天气数据的二维索引（日期+温度），可以同时按两个维度缩小范围，避免扫描其中一个维度的全部数据
### 全文检索
略
### 向量嵌入
略
## 总结
在本章中，我们试图深入了解数据库如何执行存储和检索。当你在数据库中存储数据时会发生什么，当你稍后再次查询数据时数据库会做什么？
[“分析型与事务型系统”](https://ddia.vonng.com/ch1/#sec_introduction_analytics) 介绍了事务处理（OLTP）和分析（OLAP）之间的区别。在本章中，我们看到为 OLTP 优化的存储引擎与为分析优化的存储引擎看起来非常不同：
- OLTP 系统针对大量请求进行了优化，每个请求读取和写入少量记录，并且需要快速响应。记录通常通过主键或二级索引访问，这些索引通常是从键到记录的有序映射，也支持范围查询。
- 数据仓库和类似的分析系统针对扫描大量记录的复杂读取查询进行了优化。它们通常使用带有压缩的列式存储布局，以最小化此类查询需要从磁盘读取的数据量，并使用查询的即时编译或向量化来最小化处理数据所花费的 CPU 时间。
在 OLTP 方面，我们看到了两个主要思想流派的存储引擎：
- 日志结构方法，只允许追加到文件和删除过时文件，但从不更新已写入的文件。SSTable、LSM 树、RocksDB、Cassandra、HBase、Scylla、Lucene 等属于这一组。一般来说，日志结构存储引擎往往提供高写入吞吐量。
- 就地更新方法，将磁盘视为一组可以覆盖的固定大小页。B 树是这种理念的最大例子，用于所有主要的关系型 OLTP 数据库以及许多非关系型数据库。作为经验法则，B 树往往更适合读取，提供比日志结构存储更高的读取吞吐量和更低的响应时间。
然后我们查看了可以同时搜索多个条件的索引：多维索引（如 R 树）可以同时按纬度和经度搜索地图上的点，全文检索索引可以搜索出现在同一文本中的多个关键字。最后，向量数据库用于文本文档和其他媒体的语义搜索；它们使用具有大量维度的向量，并通过比较向量相似性来查找相似文档。
# 5. 编码与演化
应用程序不可避免地会随时间而变化。在 [第 2 章](https://ddia.vonng.com/ch2/#ch_nonfunctional) 中，我们介绍了 _可演化性_ 的概念：我们应该致力于构建易于适应变化的系统（参见 [“可演化性：让变更更容易”](https://ddia.vonng.com/ch2/#sec_introduction_evolvability)）。而应用程序功能的变更也需要其存储数据的变更：可能需要捕获新的字段或记录类型，或者现有数据需要以新的方式呈现。
在大型项目中，代码更改更多是以滚动执行的方式进行，这意味着新旧版本的代码，以及新旧数据格式，可能会同时在系统中共存。为了使系统继续平稳运行，我们需要在两个方向上保持兼容性：
- 向后兼容性：较新的代码可以读取由较旧代码写入的数据。
- 向前兼容性：较旧的代码可以读取由较新代码写入的数据。
向前兼容性的另一个挑战如 [图 5-1](https://ddia.vonng.com/ch5/#fig_encoding_preserve_field) 所示。假设你向记录模式添加了一个字段，新代码创建了包含该新字段的记录并将其存储在数据库中。随后，旧版本的代码（尚不知道新字段）读取记录，更新它，然后写回。在这种情况下，理想的行为通常是旧代码保持新字段不变，即使它无法解释。但是，如果记录被解码为不显式保留未知字段的模型对象，数据可能会丢失，如 [图 5-1](https://ddia.vonng.com/ch5/#fig_encoding_preserve_field) 所示。
![](https://ddia.vonng.com/fig/ddia_0501.png)

图 5-1. 当旧版本的应用程序更新之前由新版本应用程序写入的数据时，如果不小心，数据可能会丢失。的应用程序更新之前由新版本应用程序写入的数据时，如果不小心，数据可能会丢失。
在本章中，我们将研究几种编码数据的格式，包括 JSON、XML、Protocol Buffers 和 Avro。特别是，我们将研究它们**如何处理模式变化**，以及它们**如何支持新旧数据和代码需要共存的系统**。然后我们将讨论这些格式如何用于**数据存储和通信**：在数据库、Web 服务、REST API、远程过程调用（RPC）、工作流引擎以及事件驱动系统（如 actor 和消息队列）中。
## 编码数据的格式
- 程序通常以（至少）两种不同的表示形式处理数据：
	1. 在内存中，数据保存在对象、结构体、列表、数组、哈希表、树等中。这些数据结构针对 CPU 的高效访问和操作进行了优化（通常使用指针）。
	2. 当你想要将数据写入文件或通过网络发送时，必须将其编码为某种自包含的字节序列（例如，JSON 文档）。由于指针对任何其他进程都没有意义，因此这种字节序列表示通常与内存中常用的数据结构看起来截然不同。
- 因此，我们需要在两种表示之间进行某种转换。从内存表示到字节序列的转换称为 _编码_（也称为 _序列化_ 或 _编组_），反向过程称为 _解码_（_解析_、_反序列化_、_反编组_）。
### 特定语言的格式
许多编程语言都内置了将内存对象编码为字节序列的支持。例如，Java 有 `java.io.Serializable`，Python 有 `pickle`，Ruby 有 `Marshal`，等等。许多第三方库也存在，例如 Java 的 Kryo。
但是不要将编程语言内置的序列化机制（如 Java Serializable、Python pickle、Ruby Marshal 等）用于通用、长期或跨系统的数据存储和传输。
1. **语言绑定强，跨语言困难**
    - 编码格式与特定语言深度绑定。其他语言（如 Python 读 Java 序列化数据）几乎无法解析。这会锁死技术栈，阻碍异构系统集成。
2. **安全风险高**
    - 解码时通常允许实例化任意类。攻击者可利用反序列化漏洞执行任意代码、发起远程攻击等。
3. **版本兼容性差**
    - 这类库主要目标是快速保存/恢复对象，通常缺乏对向前兼容（新代码读旧数据）和向后兼容（旧代码读新数据）的审慎设计，导致系统难以演化。
4. **性能与体积通常不佳**
    - 编码效率（CPU 时间）和编码后的大小（存储/网络开销）往往不是设计重点。例如，Java 内置序列化被公认性能差且数据臃肿。
 除了非常临时的目的（如两个相同版本的服务间短期通信或本机极短暂存储），**使用语言内置的编码通常是个坏主意**。应优先考虑跨语言、安全且注重兼容性的标准化序列化格式（如 JSON、Thrift、Protocol Buffers、Avro 等）。
### JSON、XML 及其二进制变体、
- 主要缺陷

| 问题类别          | JSON / XML / CSV 的共同/各自缺陷                                                                                                                         |
| ------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| **数字编码不精确**   | • XML/CSV：无法区分数字与数字字符串  <br>• JSON：不区分整数与浮点数，无精度控制  <br>• **大整数问题**：超过 `2⁵³` 的整数在 JavaScript 等环境中会失真（例如 Twitter 的 64 位帖子 ID 需要在 JSON 中额外用字符串传递一份） |
| **二进制数据支持差**  | • 都不支持原生字节序列  <br>• 变通方案：Base64 编码（数据体积膨胀 **33%**，且需配合模式指明编码方式）                                                                                   |
| **强依赖可选模式**   | • JSON/XML Schema 虽强大但复杂  <br>• 如果不使用模式，应用只能**硬编码**解析逻辑，无法自动区分数字/字符串/二进制                                                                          |
| **CSV 的额外问题** | • 无模式，列含义全靠应用约定  <br>• 格式模糊（逗号/换行符转义实现不一致）  <br>• 变更脆弱：增删列需人工处理                                                                                   |
| **其他**        | • XML：冗余、复杂  <br>• JSON：简单但有精度盲区  <br>• CSV：仅支持扁平的表格数据，无嵌套                                                                                        |
- 实践建议
	- **简单、异构、人类可读的场景**：JSON（首选）、XML（遗留或企业环境）、CSV（表格数据）。
	- **对精度、二进制、性能、版本兼容有要求**：不要用这些文本格式，而应转向 **二进制模式化编码**（如 Thrift、Protocol Buffers、Avro），它们将在后续章节介绍。
#### JSON 模式
- JSON 模式规范提供了许多功能。模式包括标准原始类型，包括字符串、数字、整数、对象、数组、布尔值或空值。但 JSON 模式还提供了一个单独的验证规范，允许开发人员在字段上叠加约束。例如，`port` 字段可能具有最小值 1 和最大值 65535。
- JSON 模式可以具有开放或封闭的内容模型。开放内容模型允许模式中未定义的任何字段以任何数据类型存在，而封闭内容模型只允许显式定义的字段。JSON 模式中的开放内容模型在 `additionalProperties` 设置为 `true` 时启用，这是默认值。因此，JSON 模式通常是对 _不允许_ 内容的定义（即，任何已定义字段上的无效值），而不是对模式中 _允许_ 内容的定义。
- 开放内容模型功能强大，但可能很复杂。例如，假设你想定义一个从整数（如 ID）到字符串的映射。JSON 没有映射或字典类型，只有一个可以包含字符串键和任何类型值的"对象"类型。然后，你可以使用 JSON 模式约束此类型，使键只能包含数字，值只能是字符串，使用 `patternProperties` 和 `additionalProperties`，如 [示例 5-1](https://ddia.vonng.com/ch5/#fig_encoding_json_schema) 所示。
 示例 5-1. 具有整数键和字符串值的示例 JSON 模式。整数键表示为仅包含整数的字符串，因为 JSON 模式要求所有键都是字符串。

```json
{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "patternProperties": {
        "^[0-9]+$": {
        "type": "string"
    }
    },
    "additionalProperties": false
}
```

- 除了开放和封闭内容模型以及验证器之外，JSON 模式还支持条件 if/else 模式逻辑、命名类型、对远程模式的引用等等。所有这些都构成了一种非常强大的模式语言。这些功能也使定义变得笨重。解析远程模式、推理条件规则或以向前或向后兼容的方式演化模式可能具有挑战性 [10](https://ddia.vonng.com/ch5/#fn:10)。类似的问题也适用于 XML 模式 [11](https://ddia.vonng.com/ch5/#fn:11)。
#### 二进制编码
- 示例 5-2. 本章中我们将以几种二进制格式编码的示例记录

```json
{
    "userName": "Martin",
    "favoriteNumber": 1337,
    "interests": ["daydreaming", "hacking"]
}
```
让我们看一个 MessagePack 的例子，它是 JSON 的二进制编码。[图 5-2](https://ddia.vonng.com/ch5/#fig_encoding_messagepack) 显示了如果你使用 MessagePack 编码 [示例 5-2](https://ddia.vonng.com/ch5/#fig_encoding_json) 中的 JSON 文档所得到的字节序列。前几个字节如下：
1. 第一个字节 `0x83` 表示接下来是一个对象（前四位 = `0x80`），有三个字段（后四位 = `0x03`）。（如果你想知道如果对象有超过 15 个字段会发生什么，以至于字段数无法装入四位，那么它会获得不同的类型指示符，字段数会以两个或四个字节编码。）
2. 第二个字节 `0xa8` 表示接下来是一个字符串（前四位 = `0xa0`），长度为八个字节（后四位 = `0x08`）。
3. 接下来的八个字节是 ASCII 格式的字段名 `userName`。由于之前已经指示了长度，因此不需要任何标记来告诉我们字符串在哪里结束（或任何转义）。
4. 接下来的七个字节使用前缀 `0xa6` 编码六个字母的字符串值 `Martin`，依此类推。
二进制编码长度为 66 字节，仅比文本 JSON 编码（去除空格后）占用的 81 字节少一点。所有 JSON 的二进制编码在这方面都是相似的。目前尚不清楚这种小的空间减少（以及可能的解析速度提升）是否值得失去人类可读性。
在接下来的部分中，我们将看到如何做得更好，将相同的记录编码为仅 32 字节。

[![](https://ddia.vonng.com/fig/ddia_0502.png)](https://ddia.vonng.com/ch5/#fig_encoding_json)

图 5-2. 使用 MessagePack 编码的示例记录 示例 5-2。
### Protocol Buffers
- Protocol Buffers 需要为任何编码的数据提供模式。要在 Protocol Buffers 中编码 [示例 5-2](https://ddia.vonng.com/ch5/#fig_encoding_json) 中的数据，你需要像这样在 Protocol Buffers 接口定义语言（IDL）中描述模式：

```protobuf
syntax = "proto3";

message Person {
    string user_name = 1;      // 标签 = 1
    int64 favorite_number = 2; // 标签 = 2
    repeated string interests = 3; // 标签 = 3
}
```

- Protocol Buffers 附带了一个代码生成工具，它接受像这里显示的模式定义，并生成以各种编程语言实现该模式的类。你的应用程序代码可以调用此生成的代码来编码或解码模式的记录。简而言之，写一个 **模式定义文件**（`.proto` 文件）→ 运行 **代码生成工具** → 得到各种语言的 **类** → 你的应用程序 **调用这些类** 来编码/解码数据。使用 Protocol Buffers 编码器编码 [示例 5-2](https://ddia.vonng.com/ch5/#fig_encoding_json) 需要 33 字节，如 [图 5-3](https://ddia.vonng.com/ch5/#fig_encoding_protobuf) 所示 [14](https://ddia.vonng.com/ch5/#fn:14)。

![](https://ddia.vonng.com/fig/ddia_0503.png)

图 5-3. 使用 Protocol Buffers 编码的示例记录。
- 与 [图 5-2](https://ddia.vonng.com/ch5/#fig_encoding_messagepack) 类似，每个字段都有一个类型注释（指示它是字符串、整数等）以及必要时的长度指示（例如字符串的长度）。与 [图 5-2](https://ddia.vonng.com/ch5/#fig_encoding_messagepack) 相比的最大区别是没有字段名（`userName`、`favoriteNumber`、`interests`）。相反，编码数据包含 _字段标签_，即数字（`1`、`2` 和 `3`）。这些是模式定义中出现的数字。字段标签就像字段的别名——它们是说明我们正在谈论哪个字段的紧凑方式，而无需拼写字段名。Protocol Buffers 还通过将字段类型和标签号打包到单个字节中来节省更多空间。
#### 字段标签与模式演化
模式不可避免地需要随时间而变化。我们称之为 _模式演化_。Protocol Buffers 如何在保持向后和向前兼容性的同时处理模式更改？
- **字段标签是演化核心**：编码数据只包含字段的标签号（如1、2、3）和类型，不包含字段名。因此，你可以随意修改字段的名称，但绝不能更改字段的标签号，否则现有编码数据将无法解析。
- **向前兼容（新代码写，旧代码读）**：添加新字段时，只需分配新的标签号。旧代码读取新数据时，遇到不认识的标签号会直接忽略该字段（借助类型注释跳过字节），从而保持向前兼容。
- **向后兼容（旧代码写，新代码读）**：只要标签号含义不变，新代码总能读取旧数据。如果新模式添加了字段而旧数据中没有，则新代码会为该字段填充默认值（如空字符串、零等）。
- **删除字段**：删除字段与添加字段类似，但要特别注意——永远不能再使用相同的标签号，因为旧数据中可能仍包含该标签。通常会在模式定义中保留已删除的标签号，标记为不可用，防止被误用。
- **更改数据类型**：某些类型转换是允许的，但存在风险。例如，将32位整数改为64位整数时，新代码读旧数据安全（高位补零），但旧代码读新数据时，若64位值无法装入32位变量，则会被截断，导致数据错误。
### Avro
略
## 数据流的模式
在本章开头，我们说过，当你想要将一些数据发送到与你不共享内存的另一个进程时——例如，当你想要通过网络发送数据或将其写入文件时——你需要将其编码为字节序列。然后，我们讨论了用于执行此操作的各种不同编码。

我们讨论了向前和向后兼容性，这对可演化性很重要（通过允许你独立升级系统的不同部分，而不必一次更改所有内容，使更改变得容易）。兼容性是编码数据的一个进程与解码数据的另一个进程之间的关系。

这是一个相当抽象的想法——数据可以通过许多方式从一个进程流向另一个进程。谁编码数据，谁解码数据？在本章的其余部分，我们将探讨数据在进程之间流动的一些最常见方式：
- 通过数据库（参见 [“流经数据库的数据流”](https://ddia.vonng.com/ch5/#sec_encoding_dataflow_db)）
- 通过服务调用（参见 [“流经服务的数据流：REST 与 RPC”](https://ddia.vonng.com/ch5/#sec_encoding_dataflow_rpc)）
- 通过工作流引擎（参见 [“持久化执行与工作流”](https://ddia.vonng.com/ch5/#sec_encoding_dataflow_workflows)）
- 通过异步消息（参见 [“事件驱动的架构”](https://ddia.vonng.com/ch5/#sec_encoding_dataflow_msg)）
### 流经数据库的数据流
- 在数据库中，写入数据库的进程对数据进行编码，从数据库读取的进程对其进行解码。可能只有一个进程访问数据库，在这种情况下，读取者只是同一进程的后续版本——在这种情况下，你可以将在数据库中存储某些内容视为 _向未来的自己发送消息_。因此，向后兼容性在这里显然是必要的；否则你未来的自己将无法解码你之前写的内容。
- 通常，几个不同的进程同时访问数据库是很常见的。在应用程序正在更改的环境中，某些访问数据库的进程可能正在运行较新的代码，而某些进程正在运行较旧的代码——例如，因为新版本当前正在滚动升级中部署，因此某些实例已更新，而其他实例尚未更新。这意味着数据库中的值可能由 _较新_ 版本的代码写入，随后由仍在运行的 _较旧_ 版本的代码读取。因此，数据库通常也需要向前兼容性。
#### 不同时间写入的不同值
- **数据比代码更长寿**：数据库中的旧数据可能保留多年，而应用程序版本在几分钟内就能完成替换。
- **避免重写**：大规模数据迁移成本高，因此大多数数据库会尽量避免。例如，关系数据库添加可空列时无需重写现有数据，读取时自动填充 null。
- **模式演化的表象**：通过这种方式，整个数据库看起来像由统一模式编码，即使底层存储混合了多个历史版本的模式。
- **复杂变更仍需重写**：例如单值改多值、跨表移动数据等操作，通常需要在应用层重写数据。在此类迁移中同时保持向前和向后兼容，仍是一个研究课题。
#### 归档存储
在备份或加载到数据仓库时，数据转储通常使用**最新模式**进行编码，即使原始数据库中混合了多个历史版本。既然数据已经被复制，就可以趁机统一格式——**Avro 对象容器文件**非常适合这种一次性写入、不可变的场景，同时也是转换为分析友好的列式格式（如 Parquet）的好机会。第 11 章会进一步讨论如何使用归档存储中的数据。
### 流经服务的数据流：REST 与 RPC
- **客户端与服务器**：服务器公开 API（即服务），客户端（浏览器、原生应用、JS 前端等）通过网络连接发起请求。Web 依赖于标准化协议（HTTP、HTML 等），而应用层 API 通常是特定于业务的，使用 JSON 等编码。
- **服务 vs 数据库**：数据库允许通过查询语言进行灵活查询，而服务只暴露由业务逻辑预定义的输入输出，这种限制提供了更好的封装性，可以细粒度地控制客户端行为。
- **微服务的核心目标**：服务可以独立部署和演化，每个服务由单个团队负责，能够频繁发布新版本而无需与其他团队协调。这就要求**服务 API 必须保持新旧版本（客户端与服务器）之间的数据编码兼容性**，因为不同版本会同时运行。
#### Web 服务
- **什么是 Web 服务**：使用 HTTP 作为底层协议的服务通信方式。尽管名字带“Web”，但它不仅用于网页，还广泛应用于移动应用的后端、同一数据中心内的微服务间通信，以及不同组织之间的后端数据交换（如公共 API、OAuth）。
- **主流设计理念：REST**：REST 基于 HTTP 原则设计，强调使用 URL 标识资源、简单的数据格式，并利用 HTTP 自身的功能（缓存、认证、内容协商）来实现控制。符合 REST 原则的 API 称为 RESTful。
- **接口定义语言（IDL）**：即使使用 RESTful 设计，客户端也需要知道端点和数据格式才能调用服务。OpenAPI（Swagger）和 gRPC 是目前最流行的两种服务 IDL。OpenAPI 通常配合 JSON/XML 使用，而 gRPC 底层使用 Protocol Buffers。
    示例 5-3 展示了 YAML 格式的 OpenAPI 服务定义，定义了 `/ping` 端点的 GET 请求及返回的 JSON 结构。

```yaml
openapi: 3.0.0
info:
  title: Ping, Pong
  version: 1.0.0
servers:
  - url: http://localhost:8080
paths:
  /ping:
    get:
      summary: Given a ping, returns a pong message
      responses:
        '200':
          description: A pong
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
                    example: Pong!
```
- **服务框架与实现**：开发人员需要编写代码实现 API 逻辑。框架（如 Spring Boot、FastAPI、gRPC）可以简化这一过程，处理路由、认证、缓存等通用问题。实现方式有两种：一是手写代码，框架自动生成 IDL（如 FastAPI）；二是先定义 IDL，再生成代码脚手架（如 gRPC）。
	- **实现一个 Web 服务时，服务代码和 IDL 之间的“先后关系”有两种主流方式**。下面分别解释：
	  - 方式一：手写代码，框架自动生成 IDL（如 FastAPI）
		- **做法**：你直接写 Python 代码，用 FastAPI 提供的装饰器和类（如 `@app.get`、`BaseModel`）来定义 API 和数据结构。
		- **结果**：框架会根据你的代码，**自动生成** OpenAPI 格式的 IDL 文档（即 JSON 或 YAML 格式的 API 描述文件）。
		- **一句话**：代码是源头，IDL 是副产品。
	 - 方式二：先定义 IDL，再生成代码脚手架（如 gRPC）
		- **做法**：你先写一个 `.proto` 文件（IDL），在里面定义好 service、message、rpc 方法等。
		- **结果**：用 `protoc` 工具根据 `.proto` 文件，**自动生成**各种语言的服务器端框架代码（脚手架）和客户端代码。
		- **一句话**：IDL 是源头，代码是生成的产物。
	- 为什么会有这两种方式？
		- **方式一**（代码先行）更符合很多 Web 开发者的习惯：我关心业务逻辑，文档自动帮我生成，省事。
		- **方式二**（IDL 先行）更强调“契约优先”：先让客户端和服务端团队对齐接口定义，再各自生成代码实现，避免口头约定不一致。gRPC 和 Thrift 通常采用这种模式。
	- 简单类比：
		- 方式一：先盖楼（写代码），再画图纸（生成 IDL）。
		- 方式二：先画图纸（写 IDL），再按图纸盖楼（生成代码）。
		    示例 5-4 展示了使用 Python FastAPI 框架实现示例 5-3 中定义的服务，代码中包含了路由装饰器、响应模型等。

```python
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI(title="Ping, Pong", version="1.0.0")

class PongResponse(BaseModel):
    message: str = "Pong!"

@app.get("/ping", response_model=PongResponse,
         summary="Given a ping, returns a pong message")
async def ping():
    return PongResponse()
```
- **IDL 工具的附加能力**：除了代码生成，Swagger 等 IDL 工具还能生成文档、验证模式变更的兼容性，并提供图形化界面供开发人员查询和测试服务。
#### 远程过程调用（RPC）的问题
RPC 试图让远程网络请求看起来像本地函数调用，但这种抽象从根本上是有缺陷的，因为网络请求与本地函数调用有本质区别：
- **不可预测的故障**：本地调用要么成功要么失败，而网络请求可能因超时、丢包、远程服务不可用等原因无响应，你无法区分请求是否已到达对方。
- **重试的风险**：重试失败请求时，前一个请求可能实际已成功但响应丢失，导致操作被多次执行（除非实现幂等性），而本地调用不存在此问题。
- **延迟差异巨大**：本地调用耗时稳定，网络请求则慢得多且延迟波动大，可能从不到一毫秒到几秒不等。
- **参数传递受限**：本地调用可以传递内存引用（指针），而网络请求必须将所有参数编码为字节序列，处理大数据或可变对象时很麻烦。
- **跨语言类型问题**：客户端和服务可能使用不同编程语言，类型系统差异（如 JavaScript 无法精确表示超过 2⁵³ 的整数）会导致数据转换问题。
因此，让远程服务看起来像本地对象是不切实际的。REST 的吸引力之一就在于它明确将网络上的状态传输视为与函数调用不同的过程。
#### 负载均衡器、服务发现和服务网格
服务间通信时，客户端如何找到服务地址（服务发现）以及如何分发请求（负载均衡）的几种解决方案：
- **基础方式**：手动配置 IP 和端口，但服务器变动时需要人工干预，不够灵活。
- **硬件/软件负载均衡器**：客户端连接到一个统一的地址，由负载均衡器将请求转发到后端多个实例。硬件是专用设备，软件则是 Nginx、HAProxy 等应用。
- **DNS**：通过一个域名对应多个 IP 地址来实现负载均衡，缺点是 DNS 缓存更新慢，不适合频繁变动的服务。
- **服务发现系统**：服务实例启动时自动向注册中心注册自己的地址，客户端查询注册中心获取可用实例列表后直接连接。这种方式支持动态变化，能提供更多元数据辅助智能负载均衡。
- **服务网格**：一种更复杂的方案，在客户端和服务器端分别部署本地代理（边车），所有通信都通过代理转发。优点是加密、可观测性（追踪调用链、检测故障等）可以集中在代理层处理，对业务代码透明。
**选型建议**：动态环境（如 Kubernetes）适合服务网格（Istio、Linkerd）；基础设施组件可能需要专用方案；简单场景用软件负载均衡器即可。
#### RPC 的数据编码与演化
- **简化假设**：服务端通常可以先全部更新，再更新客户端。因此只需保证**请求的向后兼容**（新客户端可以给旧服务端发请求）和**响应的向前兼容**（旧客户端能读新服务端的响应）。
- **兼容性继承自编码格式**：gRPC 依赖 Protobuf 的兼容性规则；Avro RPC 类似；RESTful API 则通过添加可选请求参数或响应字段来保持兼容。
- **跨组织场景更复杂**：服务提供方无法控制所有客户端，兼容性需要维持很长时间。破坏性变更时，通常需要同时维护多个 API 版本。
- **版本指示方式不统一**：常见做法包括在 URL 中放版本号、用 HTTP Accept 头，或为每个客户端（通过 API 密钥标识）在服务端存储其使用的版本，并通过管理界面单独控制升级。
### 持久化执行与工作流
根据定义，基于服务的架构具有多个服务，这些服务都负责应用程序的不同部分。考虑一个处理信用卡并将资金存入银行账户的支付处理应用程序。该系统可能有不同的服务负责欺诈检测、信用卡集成、银行集成等。
在我们的示例中，处理单个付款需要许多服务调用。支付处理器服务可能会调用欺诈检测服务以检查欺诈，调用信用卡服务以扣除信用卡费用，并调用银行服务以存入扣除的资金，如 [图 5-7](https://ddia.vonng.com/ch5/#fig_encoding_workflow) 所示。我们将这一系列步骤称为 _工作流_，每个步骤称为 _任务_。工作流通常定义为任务图。工作流定义可以用通用编程语言、领域特定语言 (DSL) 或标记语言（如业务流程执行语言 (BPEL)）编写。
![](https://ddia.vonng.com/fig/ddia_0507.png)

 图 5-7. 使用业务流程模型和标记法 (BPMN) 表示的工作流示例，这是一种图形标记法。
 工作流由 _工作流引擎_ 运行或执行。工作流引擎确定何时运行每个任务、任务必须在哪台机器上运行、如果任务失败该怎么办（例如，如果机器在任务运行时崩溃）、允许并行执行多少任务等。
工作流引擎通常由*编排器*和*执行器*组成。编排器负责调度要执行的任务，执行器负责执行任务。当工作流被触发时，执行开始。如果用户定义了基于时间的调度（例如每小时执行），则编排器会自行触发工作流。外部源（如 Web 服务）甚至人类也可以触发工作流执行。一旦触发，就会调用执行器来运行任务。
#### 持久化执行
持久化执行框架是为工作流提供**恰好一次语义**的一种方式。如果任务失败，框架会重新执行该任务，但会跳过失败前已成功完成的任何 RPC 调用或状态更改，而是假装进行调用并返回先前的结果。这是通过将所有 RPC 和状态更改记录到持久存储（如预写日志）实现的。
示例 5-5 展示了使用 Temporal 的支付工作流定义片段，其中包含欺诈检测、信用卡扣款等活动调用。
- 示例 5-5. [图 5-7](#fig_encoding_workflow) 中支付工作流的 Temporal 工作流定义片段。

```python
@workflow.defn
class PaymentWorkflow:
    @workflow.run
    async def run(self, payment: PaymentRequest) -> PaymentResult:
        is_fraud = await workflow.execute_activity(
            check_fraud,
            payment,
            start_to_close_timeout=timedelta(seconds=15),
        )
        if is_fraud:
            return PaymentResultFraudulent
        credit_card_response = await workflow.execute_activity(
            debit_credit_card,
            payment,
            start_to_close_timeout=timedelta(seconds=15),
        )
        # ...
```
这类框架也面临一些挑战：
- **外部服务需要幂等性**：第三方支付网关等必须提供幂等 API，开发人员需使用唯一 ID 防止重复执行。
- **代码变更脆弱**：框架按顺序记录 RPC 调用，要求后续执行以相同顺序进行相同调用。重新排序函数调用可能引入未定义行为。更安全的做法是部署新版本代码，让现有工作流继续使用旧版本，仅新调用使用新代码。
- **非确定性代码的问题**：框架期望以确定性方式重放代码（相同输入产生相同输出），随机数生成器或系统时钟等会产生问题。框架通常提供自己的确定性实现，但开发人员需要记得使用。部分框架（如 Temporal 的 `workflowcheck` 工具）还提供静态分析来判断是否引入了非确定性行为
### 事件驱动的架构
_事件驱动架构_ 是编码数据从一个进程流向另一个进程的另一种方式。请求称为 _事件_ 或 _消息_；与 RPC 不同，发送者通常不会等待接收者处理事件。此外，事件通常不是通过直接网络连接发送给接收者，而是通过称为 _消息代理_（也称为 _事件代理_、_消息队列_ 或 _面向消息的中间件_）的中介，它临时存储消息 [50](https://ddia.vonng.com/ch5/#fn:50)。
使用消息代理与直接 RPC 相比有几个优点：
- 如果接收者不可用或过载，它可以充当缓冲区，从而提高系统可靠性。
- 它可以自动将消息重新传递给已崩溃的进程，从而防止消息丢失。
- 它避免了服务发现的需要，因为发送者不需要直接连接到接收者的 IP 地址。
- 它允许将相同的消息发送给多个接收者。
- 它在逻辑上将发送者与接收者解耦（发送者只是发布消息，不关心谁使用它们）。
通过消息代理的通信是 _异步的_：发送者不会等待消息被传递，而是简单地发送它然后忘记它。可以通过让发送者在单独的通道上等待响应来实现类似同步 RPC 的模型。
#### 消息代理
- 消息代理是一种支持进程间异步通信的中间件，通过队列（点对点）或主题（发布/订阅）模式分发消息：
	- 一个进程将消息添加到命名 _队列_，代理将该消息传递给该队列的 _消费者_。如果有多个消费者，其中一个会收到消息。
	- 一个进程将消息发布到命名 _主题_，代理将该消息传递给该主题的所有 _订阅者_。如果有多个订阅者，他们都会收到消息。
- 消息本身只是字节序列，因此可以使用任何编码格式（如 Protobuf、Avro、JSON），通常会配合模式注册表来管理兼容性，AsyncAPI 可用于定义消息模式。
- 不同消息代理在持久化策略上有差异：有的将消息写入磁盘以防丢失，消费后即删除；有的可以无限期存储消息，适用于事件溯源场景。当消费者将消息重新发布到另一个主题时，需要注意保留未知字段，避免出现类似数据库上下文中的兼容性问题。
#### 分布式 actor 框架
- **Actor 模型基础**：每个 Actor 封装本地状态，通过异步消息通信，一次只处理一条消息，避免了线程相关的竞态条件和锁问题。消息传递本身不保证可靠（可能丢失）。
- **位置透明性**：无论发送者和接收者在同一节点还是不同节点，都使用相同的消息传递机制。跨节点时，消息会自动编码为字节序列、通过网络发送并解码。由于 Actor 模型本就假设消息可能丢失（即使在单进程内），因此本地与远程通信的不匹配比 RPC 少得多。
- **与消息代理的集成**：分布式 Actor 框架本质上将消息代理和 Actor 编程模型整合在一起。
- **版本兼容性**：对基于 Actor 的应用程序进行滚动升级时，新旧版本的节点之间会互相发送消息，因此仍需要通过本章讨论的编码方式（如 Protobuf、Avro 等）来保证向前和向后兼容性。
## 总结
抛开书里的复杂表述，用一个更直观的方式来理解这几个概念。
 1. 先理解一个根本问题：数据为什么要“编码”？
想象你要给远方的朋友寄一个乐高城堡。
- **城堡（内存对象）**：是你手里拼好的、立体的、不能直接塞进信封的东西。
- **编码（序列化）**：就是把城堡拆成一块块乐高砖，平铺放进一个盒子里（变成字节流），这样就能运输了。
- **解码（反序列化）**：朋友收到盒子后，照着图纸把砖块重新拼成城堡。
这个“拆开”和“重拼”的过程，就是数据编码。而**不同场景（本地调用、网络传输、长期存储），对“怎么拆、怎么拼”的要求完全不同**。
 2. 三种常见“编码方式”的区别
书里提到了好几种格式，你可以简单记住它们的特点：
- **JSON / XML**：像**明文写的信**。谁都能看懂，但啰嗦（比如字段名“userName”要写全），而且有些信息说不清（比如二进制图片没法直接写，得转成Base64，体积变大33%）。
- **MessagePack / BSON**：像**压缩过的信**。比JSON紧凑一点，但还是会把“userName”这样的字段名完整写进去。作者觉得省这点空间却丢了可读性，不划算。
- **Protobuf / Avro**：像**用暗号写的信**。收发双方提前约定好一本“暗号本”（即模式/Schema）。信里只写“1=Martin, 2=1337”，而不写“userName”这些词。**这才是真正的紧凑**（书中例子从81字节→33字节）。
 1. 核心难点：模式演化（Schema Evolution）
这是你可能最困惑的地方。什么叫“模式演化”？
 **简单说：你的软件升级了，数据格式变了，但硬盘里还存着5年前的老数据，网络上还跑着没升级的老客户端。怎么让新老数据/代码互相兼容？**
这就是**向前兼容**（新代码读旧数据）和**向后兼容**（旧代码读新数据）。
**为什么Protobuf能做好，而Java内置序列化做不好？**
- **Protobuf的秘诀：用数字标签（field tag）代替字段名。**
    - 比如你定义：`user_name = 1`
    - 数据里只存“1”，不存“user_name”。
    - 后来你给模式加了个字段 `age = 2`。
    - 老代码读到新数据：看到标签“2”，不认识，直接跳过。**没问题**（向前兼容）。
    - 新代码读到老数据：发现没有标签“2”，就给个默认值0。**也没问题**（向后兼容）。
    - **关键**：你永远不能改变或重用标签号“1”。
- **Java序列化为什么差？**
    - 它把类名、字段名、甚至代码逻辑都编码进去。
    - 一旦你改了类（比如加个字段），旧数据就再也拼不回来了（反序列化失败）。而且攻击者可以伪造字节流，让你拼出任意对象，导致安全漏洞。
 4. 不同“通信方式”对编码的要求不同
书里讲了RPC、消息代理、Actor框架，它们的区别在于**谁在说话，怎么说话**：
- **RPC（如gRPC）**：像**打电话**。客户端呼叫服务器，等待回应。问题在于网络不像本地函数调用那么可靠（可能断线、超时、重复）。**编码必须处理版本兼容**，因为客户端和服务器升级时间不同步。
- **消息代理（如Kafka）**：像**写信投递**。发送方把消息丢进队列就不管了，接收方稍后取。**编码需要长期兼容**，因为消息可能被存很久。
- **分布式Actor（如Akka）**：像**很多个收发信的小人**。每个小人（Actor）有自己的状态，互相发信。无论小人在同一台机器还是不同机器，都用同样的方式发信。**编码同样要处理兼容**，因为滚动升级时新旧版本的小人会混在一起。
 总结：
- **别用语言自带的序列化**（Java Serializable、Python pickle）——它只在玩具项目里安全，生产环境会带来兼容性和安全噩梦。
- **别盲目用JSON/XML的二进制变体**（MessagePack）——省的空间有限，却丢了可读性。
- **用带模式（Schema）的编码**（Protobuf、Avro、Thrift）——它们通过**数字标签**等机制，优雅地解决了**数据比代码活得更久**这一根本矛盾。
- **理解场景**：RPC、消息队列、Actor框架虽然用法不同，但都需要面对同一个问题：**当你升级系统时，如何让不同版本的数据和代码不打架**。这就是“模式演化”要解决的。