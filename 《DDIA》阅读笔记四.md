# 11. 批处理
- 批处理的定位：对一批有界输入做离线计算，强调吞吐量、可重跑、可回滚；在线系统则围绕请求-响应，强调低延迟与高可用。
- 批处理的输出通常是**派生数据**（见 [“记录系统与派生数据”](https://ddia.vonng.com/ch1/#sec_introduction_derived)），因此出错后常见做法是删除错误输出并重跑。
- 典型场景包括训练 AI 模型、海量数据转换、超大规模分析、ETL（见 [“数据仓库”](https://ddia.vonng.com/ch1/#sec_introduction_dwh)）。
- 批处理与流处理的分界在于输入是否有界；流处理的衔接见 [第十二章](https://ddia.vonng.com/ch12/#ch_stream)。
## 使用 Unix 工具的批处理
### 简单日志分析
- 场景是分析 nginx access.log，统计访问量最高的页面。
- 原文示例的关键点是：先提取 URL，再排序聚集相同键，再计数，再按次数取 TopN。
- 这类处理体现了批处理最基础的思路：先把“同类数据”聚到一起，再做归约。
```bash
cat /var/log/nginx/access.log | #1
  awk '{print $7}' | #2
  sort             | #3
  uniq -c          | #4
  sort -r -n       | #5
  head -n 5          #6
```
### 命令链与自定义程序
- Unix 命令链示例：`awk` 提取 URL，`sort` 聚集相同键，`uniq -c` 计数，再按计数排序后取前 5。
- 程序化示例可用散列表维护“键 -> 计数”，本质上与命令链达到相同结果。
```python
from collections import defaultdict

counts = defaultdict(int) #1

with open('/var/log/nginx/access.log', 'r') as file:
    for line in file:
        url = line.split()[6] #2
        counts[url] += 1 #3

top5 = sorted(((count, url) for url, count in counts.items()), reverse=True)[:5] #4

for count, url in top5:  #5
    print(f"{count} {url}")
```
- 两种方式的差别在于：前者依赖外部排序，后者依赖内存聚合。
### 排序与内存聚合
- **内存聚合**适合工作集能放入内存的情况，逻辑更直观。
- **外部排序**适合超内存数据，能利用磁盘顺序访问的优势（见 [“日志结构存储”](https://ddia.vonng.com/ch4/#sec_storage_log_structured) 和 [“SSD 上的顺序写与随机写”](https://ddia.vonng.com/ch4/#sidebar_sequential)）。
## 分布式系统中的批处理
- 分布式批处理框架可以看作“分布式操作系统”：它有存储层、编排层和计算层。
### 分布式文件系统
- DFS 的基本机制是把大文件切块，分散到多台数据节点上，再通过网络读取远端块。
- 元数据服务负责维护“块位置与权限”。
- 容错通常依赖多副本或纠删码；副本语义关联 [第六章](https://ddia.vonng.com/ch6/#ch_replication)。
- DFS 属于**无共享架构**（见 [“共享内存、共享磁盘与无共享架构”](https://ddia.vonng.com/ch2/#sec_introduction_shared_nothing)）。
### 对象存储
- 对象存储以 key 读写对象，常见行为是写后不可变，更新通常需要整体重写。
- 它更强调存算解耦与弹性扩展；与 DFS 的主要差异在于是否利用数据本地性调度计算。
- 对象存储与数据库边界正在融合（见 [“由对象存储支撑的数据库”](https://ddia.vonng.com/ch6/#sec_replication_object_storage)）。
### 分布式作业编排
- 作业请求通常包含任务数、CPU/内存/磁盘、镜像或代码位置、输入输出参数、凭据和硬件约束等信息。
- 核心组件包括任务执行器、资源管理器和调度器。
- 调度器的任务是决定任务放置并处理抢占；协调依赖通常借助共识型协调服务（见 [“协调服务”](https://ddia.vonng.com/ch10/#sec_consistency_coordination)）。
#### 资源分配权衡
- 调度本质上是在公平与效率之间做折中。
- 常见问题包括饥饿、抢占导致的重跑开销，以及大规模场景下接近 NP-hard 的调度复杂度。
#### 工作流调度
- 一个作业的输出可能成为多个下游作业的输入，因此批处理常以 DAG 形式组织。
- 两种常见衔接方式是：直接管道/网络传输，或者先落 DFS/对象存储再触发下游。
- 这与 [“持久化执行与工作流”](https://ddia.vonng.com/ch5/#sec_encoding_dataflow_workflows) 属于同类思想，但批处理更强调大数据离线流水线。
#### 故障处理
- 主要问题来自硬件故障、网络不可靠（见 [“硬件与软件故障”](https://ddia.vonng.com/ch2/#sec_introduction_hardware_faults) 和 [“不可靠网络”](https://ddia.vonng.com/ch9/#sec_distributed_networks)）、以及抢占式资源回收。
- 常见方案是任务级重试、血缘重算、检查点或中间结果持久化。
- 风险在于如果失败粒度退化成整作业重跑，成本会急剧上升。
## 批处理模型
### MapReduce
- MapReduce 的基本流程是：切分记录、map 产出键值、按键排序、reduce 聚合同键。
- 它把业务逻辑约束成“可并行 map + 可归约 reduce”。
- 局限在于：复杂连接表达冗长，中间落盘和阶段边界会带来额外开销。
### 数据流引擎（Spark/Flink 等）
- 数据流引擎把整条工作流作为一个作业统一优化。
- 相比 MapReduce，它不必强制每个阶段都排序，支持算子融合和前推执行，也能更充分利用内存与本地盘中的中间状态。
- 因此通常更快，也更容易编程。
### 混洗（Shuffle）
- Shuffle 的作用是把数据按键重分区并排序，使同键记录汇聚到同一个处理节点；它不是随机打乱，而是确定性重排。
- 典型过程是：map 端分桶与局部排序，reduce 端拉取并归并排序，再执行同键聚合。
- 如图11-1所示，多个 mapper/reducer 通过 shuffle 连接形成完整作业拓扑。
- ![](https://ddia.vonng.com/fig/ddia_1101.png)
#### JOIN 与 GROUP BY
- 原文示例是用户活动日志与用户画像按 user_id 连接，属于事实表与维度表的关联（星型分析模型见 [“星型与雪花型：分析模式”](https://ddia.vonng.com/ch3/#sec_datamodels_analytics)）。
- 两侧都以 user_id 为键进入 shuffle，同键在 reducer 汇合后做排序合并连接（sort-merge join），再可继续按 URL 做 group by 聚合。
- 如图11-2、图11-3所示，先连接再聚合是典型批分析路径。
- ![](https://ddia.vonng.com/fig/ddia_1102.png)
- ![](https://ddia.vonng.com/fig/ddia_1103.png)
### 查询语言与 DataFrame
- SQL 成为批处理、数据流引擎和云数据仓库的通用接口，优点是表达简洁、可交互、容易被优化器重写执行计划。
- DataFrame 是数据科学工作流的重要接口，背景见 [“DataFrame、矩阵与数组”](https://ddia.vonng.com/ch3/#sec_datamodels_dataframes)；分布式 DataFrame 通常不保留本地索引和顺序语义。
- 批处理系统与云数据仓库正在相互靠拢，交汇点见 [“云数据仓库”](https://ddia.vonng.com/ch4/#sec_cloud_data_warehouses)。
- 但复杂图迭代、部分 ML/多模态任务并不一定适合纯 SQL 表达。
## 批处理用例
### ETL/ELT
- 批处理适合并行转换、可重试和可追踪的数据集成工作流。
- 组织层面正在从中心化数据工程团队走向 data mesh / data contract 的协作方式。
### 分析（OLAP）
- 分析型负载通常包含预聚合报表和临时探索查询。
- 分析型系统范式见 [“操作型系统与分析型系统”](https://ddia.vonng.com/ch1/#sec_introduction_analytics)，预聚合视图见 [“物化视图与数据立方”](https://ddia.vonng.com/ch4/#sec_storage_materialized_views)。
### 机器学习
- 批处理在 ML 中主要用于特征工程、训练和批量推理。
- 图计算可用 BSP/Pregel 思想，图模型背景见 [“图状数据模型”](https://ddia.vonng.com/ch3/#sec_datamodels_graph)。
### 对外提供派生数据
- 批任务直接逐条写生产库的问题是：网络开销大、会冲击在线库、会破坏作业的原子可见性。
- 更好的方案是先写入流系统（如 Kafka）再由下游消费，或者采用“批构建 + 批导入 + 原子切换”。
- 若缺少“完成信号/提交语义”，下游可能读到部分结果；这种可见性问题可类比 [“读已提交”](https://ddia.vonng.com/ch8/#sec_transactions_read_committed)。
## 本章小结
- 批处理的核心是“有界输入 -> 派生输出”，因此适合重算、调试和容错。
- 分布式批处理可以抽象成三层：编排层、存储层、计算层。
- **Shuffle** 是分布式 join/group by 的基础能力，**SQL/DataFrame** 是主要接口，**DAG 编排** 是工程落地基础。
- 批处理、云数据仓库、流系统和机器学习平台在实践中正不断融合。
- 下一步自然衔接到流处理：输入从“有界”变为“无界”，系统设计会明显改变（见 [第十二章](https://ddia.vonng.com/ch12/#ch_stream)）。
# 12. 流处理
- 核心转变：批处理处理的是**有界输入**，流处理处理的是**无界输入**；前者可反复重跑，后者必须持续运行并增量处理。
- 事件流可以来自用户行为、传感器、数据库写入等；事件通常是小的、不可变的、带时间戳的自包含记录。
- 流的常见来源还包括 Unix 的 stdin/stdout、惰性列表、FileInputStream、TCP 连接，以及音视频流。
- 流处理的常见角色：把事件写入数据库 / 缓存 / 搜索索引，推送给用户，或者继续处理成新的流。
- 本章的关键主线是：如何传递事件流、如何把数据库写入当作流、如何在时间和容错条件下处理无界数据。
## 传递事件流
- 事件是流处理里的基本单位；相关事件通常被组织成 **主题（topic）** 或 **流（stream）**。
- 事件通常会被编码为文本、JSON 或二进制格式，并通过文件、数据库或网络传输。
- 生产者生成一次事件，消费者可以是一个或多个节点；这与文件“写一次、多次读”的思路相似。
### 消息传递系统
- 消息传递系统的核心问题是：生产者比消费者快时怎么办，以及节点崩溃或脱机时会不会丢消息。
- 这两个问题对应了两组常见判定：生产者过快时是丢消息、缓冲还是背压；节点故障时是能否持久化、复制、重试或恢复。
- 三种基本背压策略：丢消息、缓冲队列、背压（阻塞生产者）。
- 可靠性取决于是否持久化、是否复制，以及是否允许丢消息；对传感器和指标流，偶尔丢失可能可接受，但对计数器和交易事件就不行。
- 轮询式方案虽然简单，但在低延迟场景会产生很大开销，因此更常见的是推送式消息系统或专门的代理系统。
#### 直接从生产者传递给消费者
- 典型方式包括 UDP 组播、ZeroMQ / nanomsg、StatsD、webhooks、HTTP / RPC 直推。
- 金融行业经常用 UDP 组播做行情分发，因为低时延比绝对可靠更重要。
- 优点是低延迟、路径短；缺点是通常要求应用显式处理消息丢失，容错能力有限。
- 如果消费者脱机，生产者本地缓冲区一旦丢失，消息就无法补发，这使得这种方案对断线和崩溃更敏感。
#### 消息代理
- 消息代理把消息集中到服务器上，生产者写入代理，消费者从代理读取。
- 优点是更容易容忍客户端来来去去，持久性和缓冲都集中在代理侧；消费者通常是**异步**的。
- 生产者通常只等代理确认已缓存，不等真正被消费，因此递送时间往往是“未来某个时刻”。
- 代理可以只保存在内存，也可以落盘；落盘意味着更强持久性，但也带来排队和吞吐权衡。
- 如果消息积压过多，代理可能会把压力转移到磁盘，甚至在磁盘写满时导致故障。
#### 消息代理与数据库的对比
- 数据库更像长期保存的记录系统；消息代理更像短期的消息通道，消息通常在被消费后删除。
- 数据库支持任意查询和索引；消息代理更擅长主题订阅和模式匹配。
- 数据库查询是时间点快照；消息代理更关注“新消息到了就通知消费者”。
- 这也是为什么很多消息系统更像“为消息流优化的数据库”，而不是传统的队列。
#### 多个消费者
- **负载均衡**：每条消息只交给一个消费者，适合并行处理昂贵任务；在 AMQP 中可通过多个客户端共享一个队列，在 JMS 中叫 shared subscription。
- **扇出**：每条消息交给所有消费者，适合多个独立订阅者同时收听同一流；这相当于批处理中多个作业读同一份输入文件。
- 两种模式可以组合：多个消费者组都收到全部消息，而每个组内部再做负载均衡。
- 在工程上，这两种模式决定了资源如何共享、消息是否被多方看到，以及是否需要为不同组保留独立偏移。
#### 确认与重新传递
- 消费者要通过确认（ack）告诉代理消息已处理完成，否则代理会重传。
- 负载均衡 + 重传会导致消息顺序被打乱，如图12-2所示。
- 坏消息可能造成无限重试和资源浪费，常用死信队列（DLQ）把问题消息隔离出去；这样可以避免一个坏消息把整条消费链路堵死。
- ![](https://ddia.vonng.com/fig/ddia_1201.png)
- ![](https://ddia.vonng.com/fig/ddia_1202.png)
### 基于日志的消息代理
- 日志是仅追加的数据结构，消息代理可以把消息写入日志、让消费者按偏移量顺序读取。
- 与传统消息队列相比，基于日志的方法天然支持重播旧消息，也更适合构建派生数据系统。
- 这类系统通常把主题拆成多个分区，既保留分区内顺序，又通过多个分区获得并行度。
- Apache Kafka、Amazon Kinesis Streams、Google Cloud Pub/Sub 都属于这一类，只是暴露给用户的接口风格略有不同。
#### 使用日志进行消息存储
- 每个分区内消息有单调递增的偏移量，消费者通过记录偏移量跟踪处理进度。
- 分区提供并行度；主题由多个分区组成。
- 主题中的消息顺序只在单个分区内保证，跨分区没有全局顺序。
- Apache Kafka 和 Amazon Kinesis 是典型实现。
- 这种结构让消息代理更接近文件系统里的追加日志，而不是传统意义上的队列。
#### 日志与传统消息传递相比
- 日志型系统更适合高吞吐、顺序敏感、需要回放的场景；传统队列更适合低延迟、消息独立、处理较重的任务。
- 日志型系统能把消息长期保留，多个消费者互不影响；代价是单分区内会有头部阻塞。
- 换句话说，日志型系统更像“可回放的事件存储”，而传统队列更像“单次消费的工作分发器”。
#### 消费者偏移量
- 处理进度可以通过偏移量表示；比逐条 ack 更轻量。
- 这和数据库复制里的日志序列号非常像，消费者节点失效后可以从最后偏移量恢复。
- 代理通常只需要周期性记录消费者偏移，而不必对每条消息都维护确认状态。
#### 磁盘空间使用
- 日志会分段并周期性删除旧段，或者归档到对象存储。
- 如果消费者落后太多，就可能错过已删除的旧消息，因此需要监控滞后距离。
- 保留期越长，重放和调试越方便；但磁盘成本越高，旧数据保留策略也越复杂。
#### 重播旧消息
- 基于日志的方法天然允许从昨天的偏移量重新跑一遍消费者，适合调试、修复和再处理。
- 这让流处理更像可重复运行的批处理，只不过输入流是持续增长的。
- 这也是基于日志的消息代理特别适合组织内部做数据集成和派生数据系统的原因。
## 数据库与流
- 数据库写入本质上也是事件流；复制日志就是数据库写入事件构成的流。
- 状态机复制、数据同步、CDC 都是在把“数据库写入”当作流来消费。
- 这意味着数据库不只是“状态的存储”，也可以被理解为“事件流的当前投影”。
### 保持系统同步
- 现实系统通常有 OLTP 数据库、缓存、搜索索引、数据仓库等多个副本或派生系统，需要保持一致。
- 传统方案是 ETL 或双写；双写容易产生竞态和原子性问题，如图12-4所示。
- 如果系统里没有单一的领导者来决定写入顺序，双写问题会更严重，因为两个派生系统可能看到不同顺序的更新。
![](https://ddia.vonng.com/fig/ddia_1204.png)
图 12-4. 在数据库中 X 首先被设置为 A，然后被设置为 B，而在搜索索引处，写入以相反的顺序到达。
### 数据变更捕获（CDC）
- CDC 的目标是观察数据库写入，把变更提取成可供其他系统消费的流。
- CDC 可以用触发器、复制日志解析或专用连接器实现；Debezium、Kafka Connect 等是典型工具。
- CDC 通常是异步的，所以会继承复制延迟带来的可见性问题。
- 例如，你可以捕获数据库中的变更，并不断将相同的变更应用至搜索索引。如果变更日志以相同的顺序应用，则可以预期搜索索引中的数据与数据库中的数据是匹配的。搜索索引和任何其他派生数据系统只是变更流的消费者，如 [图 12-5](https://ddia.vonng.com/ch12/#fig_stream_cdc_flow) 所示。![692](https://ddia.vonng.com/fig/ddia_1205.png)
图 12-5. 将数据按顺序写入一个数据库，然后按照相同的顺序将这些更改应用到其他系统。
#### 初始快照
- 如果没有完整历史日志，就需要先做一致快照，再从快照对应的日志位置开始重放。
- 新建索引或新建派生系统时，这一步是必需的。
#### 日志压缩
- 日志压缩保留每个键的最新值，丢弃旧版本，适合构建数据库内容的完整副本，如 [图 12-6](https://ddia.vonng.com/ch12/#fig_stream_compaction) 所示。
- 对 key-value 型变更流特别有用，也让 Kafka 这类系统可以当作持久性存储使用。
- ![](https://ddia.vonng.com/fig/ddia_1206.png)
图 12-6. 一个键值对日志，其中键是猫视频的 ID（mew、purr、scratch、yawn），值是播放次数。日志压缩只保留每个键的最新值。
#### 变更流的 API 支持
- 越来越多数据库把变更流作为一等接口提供，而不是靠事后补丁式的工程手段提取。
- Cassandra 这类系统因为一致性模型不同，通常需要更复杂的消费方式。
#### 数据变更捕获与事件溯源
- **CDC**：应用仍按可变数据库工作，变更日志从底层抽取。
- **事件溯源**：应用直接以不可变事件为中心设计，状态是事件的派生结果。
- 两者都强调日志，但抽象层级不同；CDC更容易落地，事件溯源更彻底。
### 状态、流和不变性
- 可变状态和不可变事件日志是一体两面：状态可以看成事件流积分后的结果，事件流可以看成状态变化的导数。
- 如果永久保留变更日志，就可以重建状态；日志压缩则是在“完整历史”和“当前状态”之间取平衡。
#### 不可变事件的优点
- 不可变日志便于审计、排障、回滚和派生多个视图。
- 会计系统、版本控制系统、购物车分析等都能从这种方式中受益。
#### 从同一事件日志中派生多个视图
- 同一个事件日志可以派生出多个面向不同读取模式的视图，例如搜索索引、推荐系统、分析表和时间线缓存。
- 这就是 CQRS 的核心思想：写入和读取使用不同的表示。
#### 并发控制
- 事件溯源和 CDC 的消费者通常是异步的，因此可能出现“写入了但读取视图还没更新”的情况。
- 如果要让事件日志和读取视图同时更新，就需要事务或其他原子提交机制。
#### 不变性的局限性
- 并非所有数据都适合无限保留历史：更新 / 删除频繁时，历史会迅速膨胀。
- 隐私法规、纠错和删除需求意味着有时必须真正删除数据；crypto-shredding 是一种折中方案，但粒度有限。
## 流处理
- 流处理的三种输出：写入数据库 / 缓存 / 索引，直接推送给用户，或者继续生成新的流。
- 流处理器一般是长时间运行的算子或作业，输入无界，因此不能像批处理那样等“完成”后再统一输出。
### 流处理的应用
- 典型用途包括欺诈检测、交易风控、设备监控、告警、分析和实时物化视图维护。
#### 复合事件处理（CEP）
- CEP 用于在事件流中搜索特定模式，相当于给流写“长期运行的查询”。
- 查询先存储，事件不断流过查询；命中模式时输出复合事件。
#### 流分析
- 流分析更关注速率、平均值、百分位数、趋势等统计指标，而不只是匹配固定事件序列。
- 窗口是流分析的核心概念，常见窗口包括滚动窗口、跳动窗口、滑动窗口和会话窗口。
#### 维护物化视图
- 流处理可用于维护缓存、搜索索引和数据库派生视图。
- 对于持续变化的数据集，这类增量维护比定期全量刷新更实时也更高效。
#### 在流上搜索
- 也可以把查询本身当作长期存在的对象，把流来的文档不断拿去匹配查询，例如 percolator 风格的实时搜索。
#### 事件驱动架构与 RPC
- 事件驱动系统和 Actor 模型在通信方式上与流有交叉，但流处理更强调持久事件日志和无环流水线。
### 时间推理
- 时间推理是流处理最棘手的部分之一：处理时间、事件时间、窗口边界、延迟和乱序都必须被显式处理。
- 如果把事件流处理成分钟级速率，处理器重启或积压会让处理时间上的速率图产生假尖峰，因此应优先使用事件时间。
#### 事件时间与处理时间
- 事件时间表示事情实际发生的时间；处理时间表示流处理器看见事件的时间。
- 处理延迟、网络抖动和重试会让两者明显偏离。
#### 处理滞留事件
- 窗口“结束”后仍可能有迟到事件到达，需要决定是忽略还是更正。
- 这类滞留事件是否可接受，取决于应用对准确性和延迟的取舍。
#### 你用的是谁的时钟？
- 移动端或离线设备上事件时间通常依赖设备本地时钟，但本地时钟可能不可信。
- 可以记录三个时间戳来估计时钟偏移：事件发生、事件发送、服务器接收。
#### 窗口的类型
- **滚动窗口**：固定长度、互不重叠。
- **跳动窗口**：固定长度、允许重叠，适合平滑统计。
- **滑动窗口**：以时间差定义包含的事件集合。
- **会话窗口**：按用户活动间隔划分，没有固定长度。
- 图12-8说明了处理时间会把积压事件误判成流量尖峰。
- ![](https://ddia.vonng.com/fig/ddia_1208.png)

### 流连接
- 流处理中的连接可以分成三类：流-流、流-表、表-表。
#### 流流连接（窗口连接）
- 典型例子是搜索事件与点击事件的关联，用 session ID 在一个时间窗口内匹配。
- 需要维护状态，按会话索引最近一段时间的事件。
#### 流表连接（流扩充）
- 将活动事件流和用户档案表连接，用档案信息扩充活动事件。
- 可以直接查远程数据库，也可以把数据库副本加载到本地并通过 CDC 保持更新。
#### 表表连接（维护物化视图）
- 推特时间线是典型例子：推文和关注关系都以流表示，最终维护成每个用户的时间线缓存。
- 这类连接可以理解为物化视图的持续维护。
#### 连接的时间依赖性
- 当状态会随时间变化时，连接必须回答“按哪个时间点来连接”的问题。
- 对历史重放而言，税率、价格、维度表版本等都可能变化，因此需要版本化标识或缓慢变化维度（SCD）策略。
### 容错
- 批处理容错靠重跑失败任务；流处理是长跑，不能简单从头重来，所以需要更细粒度的恢复机制。
#### 微批次与存档点
- 微批次把流切成小块处理，适合 Spark Streaming。
- Flink 采用定期存档点和 barrier 机制，从最近检查点恢复。
- ![](https://ddia.vonng.com/fig/ddia_1207.png)
#### 原子提交再现
- 要想做到“看起来恰好处理一次”，输出、副作用、状态更新和偏移量推进必须原子一致。
- 这本质上就是流处理版的原子提交问题。
#### 幂等性
- 幂等操作重复执行与执行一次效果相同，例如把键设置成固定值。
- 结合幂等性和消息偏移量，可以避免重复写入外部系统。
#### 失败后重建状态
- 流处理器状态可以保存在远程存储中，也可以本地持有并定期复制。
- 另一个办法是从输入流重放状态，尤其在窗口较短、重建速度快时很有效。
## 本章小结
- 流处理是批处理的自然延伸：从有界输入转向无界输入。
- 消息代理、事件日志、数据库复制日志和 CDC 都是同一思想的不同表现。
- 通过日志、偏移量、窗口和状态，流系统能够把持续到来的事件转换成数据库、索引和分析视图。
- 流处理的难点主要在时间、乱序、容错和恰好一次语义。
- 处理这些问题的关键工具包括事件时间、窗口、日志压缩、存档点、事务和幂等写入。