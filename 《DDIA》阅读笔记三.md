# 第二部分：分布式数据
在本书的 [第一部分](https://ddia.vonng.com/part-i/) 中，我们讨论了数据系统的各个方面，但仅限于数据存储在单台机器上的情况。 现在我们到了 [第二部分](https://ddia.vonng.com/part-ii/)，进入更高的层次，并提出一个问题：如果 **多台机器** 参与数据的存储和检索，会发生什么？
你可能会出于各种各样的原因，希望将数据库分布到多台机器上：
**可伸缩性**：如果你的数据量、读取负载、写入负载超出单台机器的处理能力，可以将负载分散到多台计算机上。
**容错 / 高可用性**：如果你的应用需要在单台机器（或多台机器，网络或整个数据中心）出现故障的情况下仍然能继续工作，则可使用多台机器，以提供冗余。一台故障时，另一台可以接管。
**延迟**：如果在世界各地都有用户，你也许会考虑在全球范围部署多个服务器，从而每个用户可：以从地理上最近的数据中心获取服务，避免了等待网络数据包穿越半个世界。
## 伸缩至更高的负载
如果你需要的只是伸缩至更高的 **负载（load）**，最简单的方法就是购买更强大的机器（有时称为 **垂直伸缩**，即 vertical scaling，或 **向上伸缩**，即 scale up）。许多处理器，内存和磁盘可以在同一个操作系统下相互连接，快速的相互连接允许任意处理器访问内存或磁盘的任意部分。在这种 **共享内存架构（shared-memory architecture）** 中，所有的组件都可以看作一台单独的机器。
另一种方法是 **共享磁盘架构（shared-disk architecture）**，它使用多台具有独立处理器和内存的机器，但将数据存储在机器之间共享的磁盘阵列上，这些磁盘通过快速网络连接。这种架构用于某些数据仓库，但竞争和锁定的开销限制了共享磁盘方法的可伸缩性。
### 无共享架构
相比之下，**无共享架构** [3](https://ddia.vonng.com/part-ii/#fn:3)（shared-nothing architecture，有时被称为 **水平伸缩**，即 horizontal scaling，或 **向外伸缩**，即 scaling out）已经相当普及。 在这种架构中，运行数据库软件的每台机器 / 虚拟机都称为 **节点（node）**。每个节点只使用各自的处理器，内存和磁盘。节点之间的任何协调，都是在软件层面使用传统网络实现的。
### 复制 vs 分区
数据分布在多个节点上有两种常见的方式：
- 复制（Replication）
在几个不同的节点上保存数据的相同副本，可能放在不同的位置。复制提供了冗余：如果一些节点不可用，剩余的节点仍然可以提供数据服务。复制也有助于改善性能。[第六章](https://ddia.vonng.com/ch6/) 将讨论复制。
- 分区 (Partitioning)
将一个大型数据库拆分成较小的子集（称为 **分区**，即 partitions），从而不同的分区可以指派给不同的 **节点**（nodes，亦称 **分片**，即 sharding）。[第七章](https://ddia.vonng.com/ch7/) 将讨论分区。
复制和分区是不同的机制，但它们经常同时使用。如 [图 II-1](https://ddia.vonng.com/part-ii/#fig_replication_partitioning) 所示。

![](https://ddia.vonng.com/v1/ddia_part-ii_01.png)

图 II-1 一个数据库切分为两个分区，每个分区都有两个副本
理解了这些概念，就可以开始讨论在分布式系统中需要做出的困难抉择。[第八章](https://ddia.vonng.com/ch8/) 将讨论 **事务（Transaction）**，这对于了解数据系统中可能出现的各种问题，以及我们可以做些什么很有帮助。 [第九章](https://ddia.vonng.com/ch9/) 和 [第十章](https://ddia.vonng.com/ch10/) 将讨论分布式系统的根本局限性。
在本书的 [第三部分](https://ddia.vonng.com/part-iii/) 中，将讨论如何将多个（可能是分布式的）数据存储集成为一个更大的系统，以满足复杂的应用需求。但首先，我们来聊聊分布式的数据。
# 6. 复制
- 复制：指在多台通过网络连接的机器上保留相同数据的副本
- 复制的目的
    - 使数据地理上更接近用户，减少访问延迟
    - 部分组件故障时系统继续工作，提高可用性
    - 扩展处理读查询的机器数量，提高读吞吐量
- 本章假设与后续扩展
    - 本章假设：数据集足够小，每台机器可保存完整副本
    - 第7章扩展：讨论单机无法容纳的大数据集分片（分区）
- 复制的核心难点
    - 数据不变时：简单，复制一次即可
    - 数据变更时：所有困难所在，是本章主题
- 三类节点间复制变更的算法
    - 单主复制
    - 多主复制
    - 无主复制
- 复制中的主要权衡
    - 同步复制 vs 异步复制
    - 如何处理失败的副本
## 单主复制
单主复制（也称主备复制、主动/被动复制、基于领导者的复制）
- 基本概念
    - 存储数据库副本的每个节点称为**副本**
    - 核心问题：如何确保所有数据最终出现在所有副本上
    - 解决方案：每次写入都需要由每个副本处理
- 工作原理（如图 6-1 所示）
    - 指定一个副本为**领导者**（主库、源）
        - 客户端写入时，必须将请求发送给领导者
        - 领导者首先将新数据写入其本地存储
    - 其他副本称为**追随者**（只读副本、从库、热备）
        - 领导者将数据变更作为**复制日志**或**变更流**发送给所有追随者
        - 每个追随者按相同顺序应用所有写入，更新本地副本
    - 读取操作
        - 客户端可以查询领导者或任何追随者
        - 只有领导者接受写入（追随者对客户端来说是只读的）
        ![](https://ddia.vonng.com/fig/ddia_0601.png)

图 6-1. 单主复制将所有写入定向到指定的领导者，该领导者向追随者发送变更流。
### 同步复制与异步复制
- 工作原理（如图 6-1、图 6-2 所示）
    - 场景示例：网站用户更新个人资料图片
        - 客户端向领导者发送更新请求
        - 领导者收到请求
        - 领导者将数据变更转发给追随者
        - 领导者通知客户端更新成功
    - 图 6-2 示例中的两种追随者
        - 追随者1（同步复制）
            - 领导者等待追随者1确认收到写入
            - 然后才向用户报告成功
            - 然后才使写入对其他客户端可见
        - 追随者2（异步复制）
            - 领导者发送消息后不等待追随者响应
            - 追随者2处理消息前可能有相当大的延迟
![[Pasted image 20260415210605.png|717]]
图 6-2. 基于领导者的复制，带有一个同步和一个异步追随者。
- 复制的时间特性
    - 通常相当快：大多数数据库在不到一秒内将变更应用到追随者
    - 但无法保证具体时间
    - 可能落后几分钟或更长时间的情况
        - 追随者从故障中恢复
        - 系统接近最大容量运行
        - 节点之间存在网络问题
- 同步复制
    - 优点
        - 追随者保证拥有与领导者一致的最新数据副本
        - 如果领导者突然失败，数据仍然在追随者上可用
    - 缺点
        - 如果同步追随者没有响应（崩溃、网络故障等），写入无法处理
        - 领导者必须阻塞所有写入并等待同步副本再次可用
    - 实际配置
        - 将所有追随者设为同步不切实际（任何一个节点中断会导致整个系统停止）
        - 通常意味着：一个追随者是同步的，其他是异步的
        - 如果同步追随者不可用或缓慢，一个异步追随者变为同步
        - 这种配置称为**半同步**：保证至少在两个节点（领导者和一个同步追随者）上有最新数据
- 多数仲裁
    - 某些系统中，多数副本被同步更新（如5个副本中的3个），少数异步
    - 这是**仲裁**的例子（详见“读写仲裁”）
    - 多数仲裁常用于使用共识协议进行自动领导者选举的系统（详见第10章）
- 完全异步复制
    - 配置方式：基于领导者的复制被配置为完全异步
    - 风险
        - 如果领导者失败且无法恢复，尚未复制到追随者的写入会丢失
        - 即使已向客户端确认，写入也不能保证持久
    - 优点
        - 领导者可以继续处理写入，即使所有追随者都已落后
### 设置新的副本
- 背景与问题
    - 不时需要设置新的追随者（增加副本数量或替换失败节点）
    - 核心问题：如何确保新的追随者拥有领导者数据的准确副本
    - 简单复制文件不够的原因
        - 客户端不断写入数据库，数据总是在变化
        - 标准文件复制会在不同时间点看到数据库的不同部分
- 停机复制的局限性:通过锁定数据库（使其不可写入）可以使文件保持一致,但这违背了高可用性目标
- 不停机设置追随者的概念步骤
    - 步骤1：获取快照
        - 在某个时间点获取领导者数据库的**一致快照**
        - 尽可能不锁定整个数据库
        - 大多数数据库都有此功能（备份也需要）,某些情况需要第三方工具，如 MySQL 的 Percona XtraBackup
    - 步骤2：复制快照
        - 将快照复制到新的追随者
    - 步骤3：请求变更
        - 追随者连接到领导者
        - 请求自快照拍摄以来发生的所有数据变更
        - 要求快照与领导者复制日志中的确切位置相关联
        - 该位置有不同名称:
            - PostgreSQL：日志序列号
            - MySQL：binlog 位点、全局事务标识符（GTID）
    - 步骤4：追赶上进度
        - 追随者处理自快照以来的数据变更积压
        - 追上进度后，可以继续处理领导者发生的数据变更
- 扩展应用：备份与灾难恢复
    - 将复制日志归档到对象存储
    - 连同对象存储中整个数据库的定期快照
    - 示例工具
        - WAL-G：为 PostgreSQL、MySQL、SQL Server 执行此操作
        - Litestream：为 SQLite 执行等效操作
### 处理节点故障
- 目标：在单个节点故障（包括计划维护）时保持系统运行，最小化中断影响
#### 追随者故障：追赶恢复
- 追随者崩溃或网络中断后，可根据本地日志向领导者请求断开期间的所有变更来追赶恢复
- 挑战：高写入吞吐量或长时间离线会导致追赶时产生高负载；领导者需抉择是保留日志（可能耗尽磁盘）还是删除（导致无法从日志恢复）
#### 领导者故障：故障转移
- 故障转移（手动或自动）是将一个追随者提升为新领导者的过程
- 自动故障转移三步：检测领导者失效（常用超时）、选举或任命新领导者（通常选数据最新的副本）、重新配置系统让客户端使用新领导者并让旧领导者降级
- 主要风险：异步复制下新领导者可能缺失旧领导者的写入（丢弃写入会破坏持久性）；脑裂（两个节点都认为自己是领导者）可能导致数据丢失或损坏；超时设置不当可能引发不必要的故障转移或恢复时间过长
- 防止脑裂的机制称为栅栏（fencing）或STONITH[“分布式锁和租约”](https://ddia.vonng.com/ch9/#sec_distributed_lock_fencing)
- 最佳实践：优先选择最新（如同步副本或最大日志序列号）的追随者作为新领导者，以最小化数据丢失
- 节点故障、不可靠网络及一致性/持久性/可用性/延迟的权衡是分布式系统的基本问题，将在 [第 9 章](https://ddia.vonng.com/ch9/#ch_distributed) 和 [第 10 章](https://ddia.vonng.com/ch10/#ch_consistency)深入讨论
### 复制日志的实现
#### 基于语句的复制
- 领导者记录每个写入请求（SQL语句）并发送给追随者执行
- 问题：非确定性函数（NOW、RAND）、依赖执行顺序的语句、有副作用的操作可能导致副本间结果不一致
- 解决思路：用固定返回值替换非确定性调用、确定性执行（类似**状态机复制**/事件溯源）
- 使用情况：MySQL 5.1前使用，现默认在有非确定性时切换为基于行的复制；VoltDB使用并要求事务确定性，但确定性难保证
#### 预写日志（WAL）传输
- 领导者将WAL（包含磁盘块级别的字节变更细节）发送给追随者，后者重建完全相同的文件副本
- 缺点：日志与存储引擎紧密耦合，无法容忍领导者和追随者运行不同版本软件，导致升级需要停机
#### 逻辑（基于行）日志复制
- 使用独立于存储引擎的日志格式，记录行级变更：插入（新值）、删除（主键或所有旧值）、更新（标识行+新值），事务生成多条记录加提交标记
- 示例：MySQL的binlog（基于行的复制时），PostgreSQL将物理WAL解码为逻辑事件
- 优点：解耦存储引擎，支持滚动升级（不同版本软件共存），易于外部解析，用于数据变更捕获、数据仓库、自定义索引等。这种技术称为 **数据变更捕获**，我们将在 [“数据变更捕获”](https://ddia.vonng.com/ch12/#sec_stream_cdc) 一节再回到它。
## 复制延迟的问题
- 背景：基于领导者的复制中，所有写入通过领导者，只读查询可转到任何副本；通过添加异步追随者实现读扩展（增加读容量、降低延迟），因为同步复制到所有追随者不可靠（单点故障或网络中断会阻塞写入）
- 核心问题：异步追随者可能落后，导致应用程序读到过时信息，产生临时不一致（**最终一致性**——副本落后程度无理论限制，正常时延迟仅亚秒，高负载或网络问题时可达数秒甚至数分钟）
- 后续将讨论三个由复制延迟引发的问题及其解决思路
### 读己之写
- 问题场景：用户提交数据到领导者，随后从可能落后的异步追随者读取，导致看不到自己的写入，误以为数据丢失（如图6-3）
    - 定义：保证用户重新加载页面时总能看见自己提交的更新，不承诺其他用户的更新即时可见
    - 实现方法示例
        - 对用户可能修改的内容（如自己的个人资料）始终从领导者或同步追随者读取，其他内容从异步追随者读取
        - 基于时间策略：跟踪上次更新时间，在一分钟内所有读取走领导者，或监控复制延迟并避开落后过多的追随者
        - 客户端记住最近写入的时间戳或逻辑序列号，确保读取的副本至少反映该时间点之前的更新，不够新则换副本或等待追赶
    - 跨设备场景的额外复杂性
        - 不同设备间无法共享更新元数据，需要集中化存储
        - 不同设备的网络路由可能指向不同地区，如需从领导者读取，必须将所有设备请求路由到同一地区
![](https://ddia.vonng.com/fig/ddia_0603.png)

图 6-3. 用户进行写入，然后从陈旧副本读取。为了防止这种异常，我们需要写后读一致性。
### 单调读
- 问题场景：用户先从新鲜副本读到新数据，后从落后副本读到旧数据，感觉时间倒退（如图6-4）
![](https://ddia.vonng.com/fig/ddia_0604.png)

图 6-4. 用户首先从新鲜副本读取，然后从陈旧副本读取。时间似乎倒退了。为了防止这种异常，我们需要单调读。
- 定义：保证同一用户多次读取时不会出现“先读新值后读更旧值”的异常
- 实现方法：确保每个用户始终从同一副本读取（如基于用户ID哈希选择副本），若该副本失效则需重新路由
### 一致前缀读
- 问题场景：因果相关的写入（如问题和回答）因不同分片复制延迟不同，导致观察者先看到回答后看到问题（如图6-5）。这是分片（分区）数据库中的一个特殊问题，我们将在 [第 7 章](https://ddia.vonng.com/ch7/#ch_sharding) 中讨论。
![](https://ddia.vonng.com/fig/ddia_0605.png)

图 6-5. 如果某些分片的复制比其他分片慢，观察者可能会在看到问题之前看到答案。
- 定义：保证以某顺序发生的写入，任何读取都会看到相同的顺序
- 解决方案：将因果相关的写入放在同一分片，或显式跟踪因果依赖关系。还有一些算法明确跟踪因果依赖关系，这是我们将在 [“先发生关系与并发”](https://ddia.vonng.com/ch6/#sec_replication_happens_before) 中回到的主题。
### 复制延迟的解决方案
- 设计时应考虑：如果复制延迟上升到几分钟甚至几小时，应用程序表现如何
- 应用层可提供更强保证（如从领导者读取），但复杂且易错
- 更简单的方式：选择提供强一致性（如线性一致性）和ACID事务的分布式数据库（如NewSQL）
- 弱一致性仍有价值：面对网络中断时韧性更强，开销更低，后续章节将继续探讨
## 多主复制
- 背景：单主复制的主要缺点是所有写入必须通过领导者，若无法连接到领导者则无法写入
- 定义：允许多个节点接受写入，每个领导者同时充当其他领导者的追随者（主动/主动或双向复制）
- 同步与异步的选择
    - 同步多主复制：若两个领导者间网络中断，写入仍无法进行，本质上等同于单主复制，故不进一步讨论
    - 异步多主复制：任何领导者都可处理写入，即使与其他领导者的连接中断——本节重点专注于此
### 跨地域运行（多主复制的地理分布式部署）
- 场景：在不同地区部署副本，用于容灾或降低用户延迟
- [图 6-6](https://ddia.vonng.com/ch6/#fig_replication_multi_dc) 展示了这种架构：在每个地区内使用常规单主复制（追随者可能位于与领导者不同的可用区）；在地区之间，每个地区的领导者把变更复制给其他地区的领导者。

![](https://ddia.vonng.com/fig/ddia_0606.png)

图 6-6. 跨多个地区的多主复制。
- 与单主对比
    - 性能：单主需跨地区写入，延迟高；多主本地处理写入，异步复制到其他地区，感知延迟更低
    - 地区故障容忍：单主需故障转移；多主各地区可独立运行，离线地区恢复后追赶
    - 网络问题容忍：单主对跨地区链路中断敏感；异步多主可更好地容忍临时网络中断，各领导者继续处理写入
    - 一致性：单主可提供强一致性（如可串行化事务）；多主一致性弱得多，无法保证跨领导者的约束（如账户非负、用户名唯一）
- 使用情况：多主复制不如单主常见，MySQL、Oracle、SQL Server、YugabyteDB等支持，但常被视为应避免的危险领域（配置陷阱、与自增键/触发器/约束的交互问题）
#### 多主复制拓扑
- 定义：描述写入从一个节点传播到其他节点的通信路径
- 如果你有两个领导者，如 [图 6-9](https://ddia.vonng.com/ch6/#fig_replication_write_conflict) 中，只有一种合理的拓扑：领导者 1 必须将其所有写入发送到领导者 2，反之亦然。有了两个以上的领导者，各种不同的拓扑是可能的。[图 6-7](https://ddia.vonng.com/ch6/#fig_replication_topologies) 中说明了一些示例。

![](https://ddia.vonng.com/fig/ddia_0607.png)

图 6-7. 可以设置多主复制的三个示例拓扑。
- 常见拓扑
    - **全对全**：每个领导者写入发送给所有其他领导者，容错性好
    - **环形**：每个节点从一个节点接收并转发到下一个，单点故障会中断传播
    - **星形/树形**：指定根节点转发到所有其他节点
- 防止循环：每个节点有唯一标识符，写入标记经过的所有节点，收到自己标识符的变更时忽略
#### 不同拓扑的问题
- 环形/星形：单节点故障可能中断消息流，需手动重新配置        
- 全对全：网络链路速度差异可能导致消息“超越”（如先收到更新后收到插入）（如 [图 6-8](https://ddia.vonng.com/ch6/#fig_replication_causality) 所示），造成因果顺序错误；需要**版本向量**（见 [“检测并发写入”](https://ddia.vonng.com/ch6/#sec_replication_concurrent)）等技术来排序，但许多多主系统未良好实现，使用时需仔细测试
![](https://ddia.vonng.com/fig/ddia_0608.png)

图 6-8. 使用多主复制，写入可能以错误的顺序到达某些副本。
### 同步引擎与本地优先软件
- **背景**：适用于需要在断网时继续工作的应用（如手机日历）
    - 每个设备有一个本地“领导者”副本，接受读写
    - 设备之间通过**异步多主复制**同步，延迟可能达数小时或数天
    - 架构上可视为“每个设备是一个地区”，网络连接极不可靠
#### 实时协作、离线优先和本地优先应用
- 实时协作示例：Google Docs、Figma、Linear
- 用户输入立即反映在UI中，无需等待服务器往返
- 多主架构再次出现：每个浏览器选项卡是一个副本，更新异步复制给协作者
- 离线编辑与实时协作需要相同的复制基础设施：捕获更改、本地存储或发送、接收并合并更改、冲突解决
- **同步引擎**：实现上述过程的软件库
- **离线优先**：允许离线编辑的应用
- **本地优先软件**：不仅离线优先，且即使开发者关闭所有在线服务也能继续工作（如Git，通过GitHub/GitLab等同步，但不支持实时协作）
#### 同步引擎的利弊
- **优点**
    - 本地数据使UI响应更快（目标：16ms内渲染，对应60Hz刷新率）
    - 离线工作有价值，尤其在移动设备上；无需单独的“离线模式”
    - 简化前端编程模型：读写本地数据几乎从不失败，更具声明式风格
    - 结合**响应式编程**，可有效实现实时显示他人编辑
- **限制**：需要用户可能用到的所有数据提前下载并持久化；不适合超大数据量（如整个电商网站目录）
- **代表工具与历史**
    - 开创：Lotus Notes（1980年代）
    - 通用同步引擎示例：Google Firestore、Realm、Ditto（专有后端）；PouchDB/CouchDB、Automerge、Yjs（开源后端，适合本地优先软件）
- 总结：
	- 1. 先看传统 App 是怎么工作的（没有同步引擎）
		想象你用的是一个**在线协作文档**（比如普通版的 Google Docs，但假设它没有离线功能）。
		- 你输入一个字 → App 先发给服务器 → 服务器保存好 → 服务器告诉你“好了” → 屏幕才显示这个字
		- 没网的时候：你点哪里都没反应，或者提示“网络错误”
		**问题**：
		- 慢（每次操作都要等网络）
		- 断网就废了
	- **同步引擎** = 让你本地有一份数据 + 后台自动同步，实现“又快又能离线”  
	- **本地优先软件** = 用开放协议的同步引擎，公司倒闭了你还能用
### 处理写入冲突
- 例如，考虑一个维基页面同时被两个用户编辑，如 [图 6-9](https://ddia.vonng.com/ch6/#fig_replication_write_conflict) 所示。用户 1 将页面标题从 A 更改为 B，用户 2 独立地将标题从 A 更改为 C。每个用户的更改成功应用于其本地领导者。然而，当更改异步复制时，检测到冲突。这个问题在单主数据库中不会发生。

![](https://ddia.vonng.com/fig/ddia_0609.png)

图 6-9. 两个领导者并发更新同一记录导致的写入冲突。
#### 冲突避免
- 核心思路：通过设计避免冲突发生，而非事后解决
- 实现方式示例
    - 特定记录的所有写入始终通过同一领导者（对用户表现为单主）
    - 不同用户的主地区可不同（如按地理接近度选择）
    - 不同领导者使用奇偶ID范围（如一个只生成奇数ID，另一个只生成偶数ID）。我们将在 [“ID 生成器和逻辑时钟”](https://ddia.vonng.com/ch10/#sec_consistency_logical) 中讨论其他 ID 分配方案
- 风险：若允许更改记录的指定领导者（如地区不可用或用户移动），冲突避免会失效
- #### 最后写入胜利（丢弃并发写入）
- 原理：为每个写入附加时间戳，始终采用最大时间戳的值；并发写入的时间戳顺序本质随机
- 实质：随机选择一个写入作为获胜者，其他并发写入被静默丢弃
- 适用场景：可接受丢数据的场景（如仅插入不更新的记录，使用UUID作为唯一键）
- 主要问题
    - **数据丢失**：非获胜的写入被丢弃
    - 依赖时钟同步：时钟领先的节点写入更容易获胜，即使实际发生更晚
    - 解决方案：使用**逻辑时钟**，我们将在 [“ID 生成器和逻辑时钟”](https://ddia.vonng.com/ch10/#sec_consistency_logical) 中讨论
#### 手动冲突解决
- 原理：保留所有并发写入值（称为**兄弟**），查询时全部返回，由应用或用户决定如何合并，再写回数据库
- 代表系统：CouchDB、Git（版本控制系统）
- 主要问题
    - API改变：简单类型可能变成集合，增加应用复杂度
    - 用户负担重：需构建冲突解决界面，用户可能不理解
    - 自动合并可能反直觉：如亚马逊购物车取并集导致已删除商品“复活”（如图6-10所示：设备 1 删除 Book，设备 2 并发删除 DVD，冲突合并后两个商品都回来了）
    - 并发解决冲突可能引入新冲突或导致不一致（如“B/C” vs “C/B”进一步合并成“B/C/C/B”）
![](https://ddia.vonng.com/fig/ddia_0610.png)

图 6-10. 亚马逊购物车异常的示例：如果购物车上的冲突通过取并集合并，删除的项目可能会重新出现。
#### 自动冲突解决
- 目标：所有副本收敛到相同状态，尽可能保留所有更新的预期效果，避免数据丢失
- 常见合并算法（按数据类型）
    - **文本**：跟踪字符插入和删除，合并保留所有改动；同一位置并发插入用确定性顺序排序
    - **集合**（如购物车）：跟踪插入和删除，避免已删商品复活（解决图6-10问题）
    - **计数器**：统计每个兄弟上的增减次数并正确求和
    - **键值映射**：对同一键的值递归应用上述算法之一
- 局限性：受限于约束（如列表最多5项，并发添加超过5项时必须丢弃部分数据）
- 结论：对于离线优先或本地优先软件，冲突解决不可避免，自动化通常是最佳方法
### CRDT 与操作变换
- 定位：两个主流的**自动冲突解决**算法族，用于合并并发更新
- 共同目标：为文本、集合、计数器、键值映射等数据类型执行自动合并
- 核心区别：设计理念与性能特征不同，但均能使副本收敛到一致状态
- 示例（文本合并，如图6-11）
    - 初始状态："ice"
    - 并发操作：副本1在开头加"n"（→"nice"），副本2在结尾加"!"（→"ice!"）
    - 期望合并结果："nice!"
![](https://ddia.vonng.com/fig/ddia_0611.png)

图 6-11. OT 和 CRDT 如何分别合并对字符串的两个并发插入。
- **OT**
    - 原理：记录插入/删除的**索引**，交换操作后**转换索引**以考虑已应用的并发操作
    - 处理：在"ice"的索引0插入"n"得到"nice"；原计划在索引3插入"!"需转换为索引4，避免得到"nic!e"。因此，我们需要转换每个操作的索引以考虑已经应用的并发操作；在这种情况下，"!“的插入被转换为索引 4 以考虑在较早索引处插入"n”。
    - 典型应用：实时协作编辑（如 **Google Docs**）
- **CRDT**
    - 原理：为每个字符分配**唯一、不可变的ID**，基于ID而非索引确定插入位置
    - 处理：插入操作包含新字符ID和前后字符ID；同一位置的并发插入按字符ID排序，无需转换
    - 典型应用：分布式数据库（如 **Redis Enterprise、Riak、Azure Cosmos DB**）
    - 同步引擎示例：**Automerge、Yjs**（JSON数据）
- 补充说明
    - 列表/数组、键值映射等数据类型可类似支持
    - OT与CRDT存在性能与功能权衡，也可结合两者优点
    - OT同步引擎示例：**ShareDB**
#### 什么是冲突？
- 显式冲突：并发修改同一记录的同一字段为不同值（如图6-9）
- 隐式冲突示例：会议室预订系统 – 同一房间同时创建两个重叠预订，即使各自领导者上检查可用性也无法避免
- 我们将在 [第 8 章](https://ddia.vonng.com/ch8/#ch_transactions) 看到更多冲突案例，并在 [“通过事件顺序捕获因果关系”](https://ddia.vonng.com/ch13/#sec_future_capture_causality) 中讨论在复制系统里可伸缩地检测和解决冲突的方法。
## 无主复制
- 到目前为止，我们在本章中讨论的复制方法——**单主和多主复制**——都基于这样的想法：客户端向一个节点（领导者）发送写入请求，数据库系统负责将该写入复制到其他副本。领导者确定写入应该处理的顺序，追随者以相同的顺序应用领导者的写入。
- 在某些无主实现中，客户端直接将其写入发送到多个副本，而在其他实现中，协调器节点代表客户端执行此操作。然而，与领导者数据库不同，该协调器不强制执行特定的写入顺序。这种数据库也被称为 **Dynamo 风格**。
### 当节点故障时写入数据库
- 核心机制：**无故障转移**，客户端并行写入所有副本，等待足够数量的确认即认为成功
- 示例（n=3，如图6-12）：写入并行发往3个副本，2个确认成功即算写入完成，忽略不可用副本。客户端（用户 1234）将写入并行发送到所有三个副本，两个可用副本接受写入，但不可用副本错过了它。假设三个副本中有两个确认写入就足够了：在用户 1234 收到两个 **ok** 响应后，我们认为写入成功。客户端只是忽略了其中一个副本错过写入的事实。

![](https://ddia.vonng.com/fig/ddia_0612.png)

图 6-12. 节点中断后的仲裁写入、仲裁读取和读修复。
- 问题：恢复后的节点缺失写入，读取可能返回**陈旧值**
- 解决方案：并行读取多个节点，用版本号/时间戳取最新值，类似于我们在 [“最后写入胜利（丢弃并发写入）”](https://ddia.vonng.com/ch6/#sec_replication_lww) 中看到的。
#### 追赶错过的写入
- **读修复**（如图6-12）：客户端并行读取时检测陈旧响应，将较新值写回陈旧副本；适用于频繁读取的值
- **提示移交**：可用副本代表不可用副本存储写入（作为“提示”）；待后者恢复后转发写入并删除提示；可覆盖未被读取的值
- **反熵**：后台进程定期比对副本间数据差异并复制缺失数据；无特定顺序，延迟可能较大
#### 读写仲裁
- 参数定义
    - **n**：副本总数（某值的存储节点数，通常为3或5）
    - **w**：每次写入需确认的节点数
    - **r**：每次读取需查询的节点数
- 仲裁条件：**w + r > n** → 读取的r个副本中至少有一个包含最新写入
- 常见配置：n为奇数，w = r = (n+1)/2（向上取整）
    - n=3, w=2, r=2 → 可容忍1个节点不可用（如图6-12）
    - n=5, w=3, r=3 → 可容忍2个节点不可用（如图6-13）
![](https://ddia.vonng.com/fig/ddia_0613.png)

图 6-13. 如果 _w_ + _r_ > _n_，你读取的 _r_ 个副本中至少有一个必须看到最近的成功写入。
- 灵活调整示例：读多写少场景可设w=n, r=1（读取快，但单节点故障导致所有写入失败）
- 错误处理：若可用节点数 < w 或 < r，则写入或读取返回错误
- 补充说明
    - 读取/写入通常并行发往所有n个副本，w/r决定等待多少个成功响应
    - 集群节点数可大于n，用于分片（详见第7章）
### 仲裁一致性的局限
- 基本保证：w + r > n 时，通常期望每次读取返回最新写入值（读写节点集必有重叠）
- 参数灵活性
    - 多数派（> n/2）是常见选择，可容忍最多 ⌊n/2⌋ 个节点故障
    - 也可设 w + r ≤ n：延迟更低、可用性更高，但更易读到陈旧值
    - 仅当可访问副本数 < w 或 < r 时，数据库才无法写入或读取
- **边缘场景：即使 w + r > n，一致性仍可能出问题**
    - 携带新值的节点故障并从旧值副本恢复 → 新值副本数可能低于 w
    - 数据重新平衡时，节点对 n 个副本的归属视图不一致 → 读写仲裁可能不再重叠
    - 读取与写入并发：可能看到新值，后续读取又看到旧值（详见“线性一致性与仲裁”）
    - 写入在少于 w 个副本上成功但报告失败 → 后续读取可能返回也可能不返回该写入
    - 使用实时时钟时间戳（如 Cassandra）：时钟快的节点写入可能静默丢弃更晚的实际写入
    - 并发写入导致冲突（类似多主复制）
- 结论：仲裁并非绝对保证，Dynamo 风格数据库针对**可容忍最终一致性**的场景优化，w/r 仅调整陈旧读概率
#### 监控陈旧性
- 必要性：即使应用容忍陈旧读，也需了解复制健康状况，明显落后时应告警
- **基于领导者的复制**：易监控（写入顺序固定），通过领导者与追随者的复制日志位置差测量延迟
- **无主复制**：无固定写入顺序，监控更困难；提示移交数量可作为健康度量之一，但难以直观解释
- 核心挑战：最终一致性是模糊保证，实践中需量化“最终”的程度
### 单主与无主复制的性能
- **单主复制的性能问题**
    - 强一致性：从领导者读取保证最新响应
    - 性能瓶颈
        - 读取吞吐量受限于领导者处理能力（无法水平扩展读）
        - 领导者故障需等待检测和故障转移，期间用户感知延迟或不可用
        - 领导者过载或资源争用会立即影响所有用户响应时间
- **无主复制的优势：弹性**
    - 无故障转移，请求并行发往多个副本
    - 单个副本变慢或不可用影响小：客户端采用最快响应（**请求对冲**），显著降低尾部延迟
    - 核心原因：不区分正常与故障情况，对**灰色故障**（节点降级、异常缓慢）或过载节点尤其有效
- **无主复制的性能问题**
    - 需检测不可用副本并存储**提示移交**，恢复时额外负载加重系统压力
    - 副本数越多，仲裁（r/w）越大，遇到慢副本的概率越高 → 总体响应时间增加（见 [“响应时间指标的应用”](https://ddia.vonng.com/ch2/#sec_introduction_slo_sla)）。
    - 大规模网络中断可能导致无法形成仲裁
        - 解决方案：**宽松仲裁**（Riak/Dynamo）或**一致性级别 ANY**（Cassandra/ScyllaDB），允许任意可访问副本接受写入，但后续读取不保证能看到该写入
- **多主复制的定位**
    - 相比无主：对更大范围网络中断弹性更好（读写只需与同位置的一个领导者通信）
    - 缺点：领导者间异步传播，读取可能任意过时
- 总结：仲裁读写提供折衷——良好的容错性 + 读最新数据的高概率
#### 多地区操作
- **无主复制（Cassandra/ScyllaDB）**
    - 客户端直接向所有地区的副本发送写入
    - 一致性级别可选：全地区仲裁、每地区单独仲裁、仅客户端本地地区仲裁
    - 本地仲裁避免跨地区慢请求，但更可能返回陈旧结果
- **Riak 的方式**
    - 客户端与数据库节点通信限制在本地地区（n 为地区内副本数）
    - 跨地区复制：后台异步进行，风格类似**多主复制**
### 检测并发写入
- 问题背景：无主数据库中，并发写入可能在不同节点以不同顺序到达（如图6-14），若直接覆盖会导致节点间永久不一致
	- 节点 1 接收来自 A 的写入，但由于瞬时中断从未接收来自 B 的写入。
	- 节点 2 首先接收来自 A 的写入，然后接收来自 B 的写入。
	- 节点 3 首先接收来自 B 的写入，然后接收来自 A 的写入。

![](https://ddia.vonng.com/fig/ddia_0614.png)

图 6-14. Dynamo 风格数据存储中的并发写入：没有明确定义的顺序。
- 解决方案：使用冲突解决机制（LWW、手动解决、CRDT（在 [“CRDT 与操作变换”](https://ddia.vonng.com/ch6/#sec_replication_crdts) 中描述，并由 Riak 使用））使副本收敛
- 关键前提：需先判断两个操作是**先后关系**还是**并发关系**
#### “先发生"关系与并发
- **先发生**：操作B知道、依赖或建立在操作A之上 → A先发生于B（存在因果依赖） [图 6-8](https://ddia.vonng.com/ch6/#fig_replication_causality)
- **并发**：两个操作互不知道对方 → 两者均不先发生于另一个[图 6-14](https://ddia.vonng.com/ch6/#fig_replication_concurrency)
- 三种可能性：A先发生于B / B先发生于A / A与B并发
- 与物理时间无关：取决于信息能否传递（网络延迟或中断可导致并发，即使时间上相隔）
#### 捕获先发生关系（单副本版）
- 核心机制：为每个键维护**版本号**，每次写入递增
- 读写流程
    - 读：服务器返回**所有兄弟**（未被覆盖的值）+ 最新版本号
    - 写：客户端必须携带上次读取的版本号 + 合并所有读取到的值
- 服务器的处理规则
    - 收到带版本号的写入 → 覆盖该版本号及更低版本的所有值
    - 保留更高版本号的所有值（与当前写入并发）
    - 不带版本号的写入 → 与所有其他写入并发，不覆盖任何内容，仅作为新兄弟返回
- 示例：图6-15（购物车并发添加） + 图6-16（因果依赖关系图）
[图 6-15](https://ddia.vonng.com/ch6/#fig_replication_causality_single) 显示了两个客户端并发地向同一购物车添加项目。（如果这个例子让你觉得太无聊，想象一下两个空中交通管制员并发地向他们正在跟踪的扇区添加飞机。）最初，购物车是空的。两个客户端总共向数据库发起了五次写入：
1. 客户端 1 将 `milk` 添加到购物车。这是对该键的第一次写入，因此服务器成功存储它并为其分配版本 1。服务器还将值连同版本号一起回显给客户端。
2. 客户端 2 将 `eggs` 添加到购物车，不知道客户端 1 并发地添加了 `milk`（客户端 2 认为它的 `eggs` 是购物车中的唯一项目）。服务器为此写入分配版本 2，并将 `eggs` 和 `milk` 存储为两个单独的值（兄弟）。然后，它将 **两个** 值连同版本号 2 一起返回给客户端。
3. 客户端 1，不知道客户端 2 的写入，想要将 `flour` 添加到购物车，因此它认为当前购物车内容应该是 `[milk, flour]`。它将此值连同服务器之前给客户端 1 的版本号 1 一起发送到服务器。服务器可以从版本号判断 `[milk, flour]` 的写入取代了 `[milk]` 的先前值，但它与 `[eggs]` 并发。因此，服务器将版本 3 分配给 `[milk, flour]`，覆盖版本 1 值 `[milk]`，但保留版本 2 值 `[eggs]` 并将两个剩余值返回给客户端。
4. 同时，客户端 2 想要将 `ham` 添加到购物车，不知道客户端 1 刚刚添加了 `flour`。客户端 2 在上次响应中从服务器接收了两个值 `[milk]` 和 `[eggs]`，因此客户端现在合并这些值并添加 `ham` 以形成新值 `[eggs, milk, ham]`。它将该值连同先前的版本号 2 一起发送到服务器。服务器检测到版本 2 覆盖 `[eggs]` 但与 `[milk, flour]` 并发，因此两个剩余值是版本 3 的 `[milk, flour]` 和版本 4 的 `[eggs, milk, ham]`。
5. 最后，客户端 1 想要添加 `bacon`。它之前从服务器接收了版本 3 的 `[milk, flour]` 和 `[eggs]`，因此它合并这些，添加 `bacon`，并将最终值 `[milk, flour, eggs, bacon]` 连同版本号 3 一起发送到服务器。这覆盖了 `[milk, flour]`（注意 `[eggs]` 已经在上一步中被覆盖）但与 `[eggs, milk, ham]` 并发，因此服务器保留这两个并发值。

![](https://ddia.vonng.com/fig/ddia_0615.png)

图 6-15. 捕获两个客户端并发编辑购物车之间的因果依赖关系。
[图 6-15](https://ddia.vonng.com/ch6/#fig_replication_causality_single) 中操作之间的数据流在 [图 6-16](https://ddia.vonng.com/ch6/#fig_replication_causal_dependencies) 中以图形方式说明。箭头指示哪个操作 **先发生于** 哪个其他操作，即后面的操作 **知道** 或 **依赖于** 前面的操作。在这个例子中，客户端从未完全了解服务器上的数据，因为总是有另一个并发进行的操作。但是值的旧版本最终会被覆盖，并且不会丢失任何写入。

[![](https://ddia.vonng.com/fig/ddia_0616.png)](https://ddia.vonng.com/ch6/#fig_replication_causality_single)

图 6-16. 图 6-15 中因果依赖关系的图。
#### 版本向量（多副本无主场景）
[图 6-15](https://ddia.vonng.com/ch6/#fig_replication_causality_single) 中的示例只使用了单个副本。当存在多个副本、且没有领导者时，算法如何变化？
[图 6-15](https://ddia.vonng.com/ch6/#fig_replication_causality_single) 使用单个版本号来捕获操作间依赖关系，但当多个副本并发接受写入时，这还不够。我们需要为 **每个副本**、每个键分别维护版本号。每个副本在处理写入时递增自己的版本号，并追踪从其他副本看到的版本号。这些信息决定了哪些值该被覆盖，哪些值要作为兄弟保留。
来自所有副本的版本号集合称为 **版本向量**。这一思想有若干变体，其中较有代表性的是 **点版本向量** ，Riak 2.0 使用了它。这里不展开细节，它的工作方式与前面的购物车示例非常相似。
和 [图 6-15](https://ddia.vonng.com/ch6/#fig_replication_causality_single) 里的版本号一样，版本向量会在读取时由数据库副本返回给客户端，并在后续写入时再由客户端带回数据库。（Riak 把版本向量编码成一个字符串，称为 **因果上下文**。）版本向量让数据库能够区分“覆盖写入”和“并发写入”。
版本向量还保证了“从一个副本读取，再写回另一个副本”是安全的。这样做可能会产生兄弟，但只要正确合并兄弟，就不会丢失数据。
## 总结
- **复制的目的**
	- **高可用性**：容忍机器、区域甚至整个地区宕机
	- **断开操作**：允许网络中断时继续工作
	- **延迟**：将数据置于靠近用户的地理位置
	- **可伸缩性**：通过副本读取处理超出单机能力的读取量
    - **核心挑战**：并发控制、故障处理（节点不可用、网络中断、静默数据损坏等隐蔽故障）
- **三种复制方法对比**
    - **单主复制**
        - 所有写入发往单个领导者，变更事件流发送给追随者
        - 读取可在任意副本执行（追随者读取可能陈旧）
        - 优点：易于理解，可提供强一致性
    - **多主复制**
        - 多个领导者均可接受写入，领导者之间互发变更事件流
        - 优点：对故障、网络中断和延迟峰值更具弹性
        - 代价：需要冲突解决，提供较弱的一致性保证
    - **无主复制**
        - 写入并行发往多个节点，读取也从多节点并行获取以检测和纠正陈旧数据
        - 优点：弹性好
        - 代价：冲突解决，较弱一致性
- **同步 vs 异步复制**
    - 异步复制在平稳时很快，但领导者故障时可能丢失最近提交的数据
- **复制延迟下的一致性模型**
    - **写后读一致性**：用户始终看到自己提交的数据
    - **单调读**：用户不会从较早时间点看到数据
    - **一致前缀读**：用户看到因果一致的状态（如问题与回复的正确顺序）
- **最终收敛机制（多主/无主）**
    - 使用**版本向量**或类似算法检测并发写入
    - 使用冲突解决算法合并并发值（如 **CRDT**、LWW、手动解决）